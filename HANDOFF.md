# LifeOS Agent — Handoff (2026-08-23, ночь #4)

Вставьте этот файл целиком в начало нового чата с Claude, чтобы
продолжить работу без потери контекста. Ниже раздела «более ранние
записи» — история, она не пересматривалась.

Прод: коммит `54dc15e6`, `bash lifeos-agent/scripts/deploy.sh --check`
зелёный, 1056 тестов, ruff и black чистые. Миграция
`021_create_assistant_settings` прогнана на реальном проде, `/health` и
`/ui/` отвечают (проверено curl'ом сразу после деплоя), новый роут
`/assistant/persona` зарегистрирован (без токена отвечает 401, не 404 —
подключён к общей защите).

> **Персонажи бота — фаза 1 реализована и задеплоена в эту сессию**
> (специфика ниже, раздел 6 «Что сделано»), продолжение обсуждения с
> владельцем о том, что Telegram должен ощущаться живым собеседником, а
> не только списком команд. Четыре персонажа (дворецкий/тренер/директор/
> финансист), переключатель только на `/ui`, голос красит пять AI-мест
> (брифинг/вечерний вопрос/финотчёт/дайджест/новый разговорный фолбэк).
> **Живой проверки владельцем НЕ было** — ни голоса персонажа в реальном
> Telegram, ни переключателя на `/ui` глазами. Первое, что стоит сделать
> в начале следующей сессии — см. п.0 ниже.

> **Картинки скинов ЗАКРЫТЫ — 29 из 30, старый главный блокер снят.**
> Владелец сгенерировал все 30 в тот же вечер (папка `Стили/`, не в
> гите — добавлена в `.gitignore`), в этой же сессии обработаны
> (`lifeos-agent/scripts/process_skin_images.py`: `pngquant` на машине
> не установлен, сжатие через Pillow) и подключены в CSS всех четырёх
> скинов. graffiti и pixel из «пока картинок нет, слот готов, ждёт»
> перешли в «полностью иллюстрированы», у всех четырёх появилась
> картинка на карточке финансов. Единственное, что осталось —
> **перегенерировать `graffiti/logo.png`**: пришёл с текстом
> `LIFEOS TRACKER` вместо одного слова `LIFEOS` (нарушение промпта),
> файл удалён, промпт лежит в `designs/002` (файл теперь об одной
> оставшейся картинке, а не о тридцати). Подробности — раздел 5 в «Что
> сделано».

> **Личный CRM и трекер настроения — оба реализованы и задеплоены
> в этот же вечер**, третьим и четвёртым пунктом из «Дальше по списку»
> прошлой версии файла (фокус-сессии/Pomodoro — не начаты, см. ниже).
> Оба сделаны сразу с REST API и `/ui`-карточкой, не отложены на потом —
> прямой урок из истории с финансами (см. ниже в блоке про них).
> Подробности — раздел 4 в «Что сделано», `specs/018-personal-crm.md`,
> `specs/019-mood-tracker.md`.

> **Живой проверки владельцем (глазами и руками в Telegram/на `/ui`)
> по-прежнему не было** ни по personal CRM, ни по трекеру настроения,
> ни по картинкам скинов, ни по прошлым пунктам (engagement hooks,
> финансы) — CSS-подключение картинок проверено функционально (HTTP 200
> на все файлы + computed `background-image` совпадает с ожидаемым для
> каждого скина, на самом проде через JS в браузере), но **полноценного
> скриншота ни разу не снято** — Browser pane не был в фокусе клиента
> всю сессию, `agent-browser` зависал на установке. Стоит открыть
> [lifeos-agent.ru/ui](https://lifeos-agent.ru/ui/) глазами и
> переключить все четыре скина — это первое, что стоит сделать в начале
> следующей сессии.

> **Голосовой ввод по-прежнему ВЫКЛЮЧЕН намеренно** (`voice_input_enabled=false`).
> Бот на голосовые молчит — так задумано, чинить нечего. Перед обратным
> включением нужен счётчик минут на пользователя и лимит.

> **Автоматической генерации изображений по-прежнему НЕТ** (MCP «Nano
> Banana» не подключён, `OPENAI_API_KEY` не задан) — картинки этой
> сессии владелец сгенерировал сам, вручную, тем же способом, что и
> раньше. Если понадобятся ещё картинки (например, для будущих
> доменов) — тот же процесс: владелец генерит по промпту → `python
> lifeos-agent/scripts/process_skin_images.py <папка> <скин>`.

---

## Что сделать в начале новой сессии

**0. Проверить персонажей бота живьём — реализовано, но НИ РАЗУ не
увидено.** Открыть [lifeos-agent.ru/ui](https://lifeos-agent.ru/ui/) →
«Настройки» → переключить «Персонаж» на «Тренер» (или любой другой),
убедиться что сохраняется (значение не сбрасывается после перезагрузки
страницы) и что тон в утреннем брифинге/вечернем вопросе/финотчёте/
дайджесте реально меняется. Отдельно — написать боту что-то с «?» на
конце или «привет»/«как дела» и посмотреть, отвечает ли персонаж по
существу вместо создания задачи с этим текстом как заголовком (см.
specs/020-butler-personas.md, `Intent.CHAT`). Если голос не чувствуется
(слишком похоже на дефолтного дворецкого) или разговорный ответ ведёт
себя странно — почитать `app/assistant/personas.py` и
`app/conversation/chat_reply.py`, донастроить характер-листы.

**1. `/ui` наконец увидено глазами — Browser pane заработал в эту
сессию.** Все четыре скина (graffiti/pixel/groovy/japan) открыты и
переключены на [lifeos-agent.ru/ui](https://lifeos-agent.ru/ui/) —
визуальных багов не найдено, консоль чистая на всех четырёх. Карточка
«Ежедневный визит» показывает мотивационную иллюстрацию у всех
четырёх, «Финансы» — угловой герой. `graffiti`/`pixel` больше не
пустые. У graffiti отдельно проверено через JS (`getComputedStyle` +
`fetch HEAD`) — все 5 `.empty`-картинок и герой финансов резолвятся и
отвечают 200 (сам список задач/привычек не показать без
`telegram_user_id` — это ожидаемо, не баг). **Осталось: перегенерировать
`graffiti/logo.png`** (сейчас там текст `LIFEOS TRACKER` вместо
`LIFEOS`, поэтому в шапке видно кольцо `.brand-mark`, а не леттеринг —
подтверждено визуально, соответствует ожиданию) — единственный
оставшийся промпт лежит в `designs/002-skin-illustration-prompts.md`,
положить готовый файл в `skins/graffiti/logo.png` и включить
`.brand::before` (комментарий на месте в `index.html` — что именно
раскомментировать/добавить, там же).

**2. Проверить руками всё, что сделано за 23.08 — ничего живьём не
подтверждено (REST API проверен на проде напрямую, это не заменяет
реальное использование через бота/`/ui` глазами владельца):**
- engagement hooks: не рвётся ли утренний брифинг от вопроса снизу,
  приходит ли «🪙 +N» туда, где ожидается, не мешает ли кнопка
  «⭐ Сохранить» читать дайджест;
- финансы: и текстом («потратил 500 на такси»), и кнопкой «💰 Финансы» в
  меню бота, и на `/ui` (вкладка «Финансы») — три входа в одну и ту же
  фичу; дождаться воскресенья (или подвинуть
  `finance_report_hour`/`_minute` вручную) и посмотреть на реальный
  еженедельный отчёт с настоящими категориями;
- **личный CRM**: пункт меню «📇 Люди» в боте (добавить контакт с датой
  рождения — «Аня 14.09» — и без неё, отметить «написал(а)», удалить),
  карточка на `/ui`; нэджи (день рождения за 3/1/0 дней, «давно не
  писал» на 30-й день) реальным временем не проверялись вообще — при
  желании подвинуть системную дату или дату контакта для быстрой
  проверки `app/scheduler/nudges.py`;
- **трекер настроения**: 5 эмодзи-кнопок под вечерним дневниковым
  вопросом в 21:00 (не мешают ли отвечать на сам вопрос текстом), пункт
  меню «😊 Настроение», карточка на `/ui`; график настроения в
  еженедельном дайджесте — увидеть можно только в реальное воскресенье
  (или руками подвинуть `weekly_digest`-джобу) и то только если к этому
  моменту накопится хоть одна отметка.

**3. Дальше — фокус-сессии/Pomodoro** — последнее из трёх, что владелец
просил, но ещё не начато. Уже отмечено в этом файле как самое тонкое из
трёх (не решение владельца, а наблюдение прошлой сессии): Telegram-бот
не может блокировать приложения, как настоящие конкуренты (Reclaim/
Motion). Реалистичный масштаб — таймер+лог (бот шлёт "начали" → через
N минут "закончили, был перерыв?", логирует сессии для статистики), не
блокировщик — это предложенный, не подтверждённый владельцем скоуп,
стоит уточнить перед реализацией (в отличие от CRM/mood, где решения
были ясны из формулировки задачи и реализация пошла без уточнений).

> **Раскладка общая для всех скинов** (`designs/004-ui-layout.md`), скин
> её не трогает. **Образцом брать ГРУВИ**, японский однажды тоже стоит
> привести к системе 003.

> **Превью `index.html` через `file://` не работает для кнопок,
> дёргающих API** — смотреть интерактив на `http://localhost:8000/ui`
> с поднятым бэкендом или на проде.

---

## Что сделано в этой сессии (23.08, ночь #4)

### 6. Персонажи бота — фаза 1 (коммит `54dc15e6`, specs/020-butler-personas.md)

Владелец захотел, чтобы Telegram ощущался живым собеседником с
характером, а не только списком команд — продолжение разговора про
низкую вовлечённость (specs/016-engagement-hooks.md по-прежнему
актуален: реально читаются только дайджесты каналов). Договорились
через несколько раундов уточнений (детали — в самой спеке): без голоса
(дорого, уже отклонено для voice input), без буквальных персонажей из
книг/игр/фильмов (копирайт + разностилье), переключатель только на
сайте, частоту push НЕ увеличивать (specs/016 уже сокращал число
слотов) — вместо этого существующие AI-вставки стали практичнее и
длиннее (2-4 предложения вместо одной фразы на 20 слов).

Восьмой домен `app/assistant/` — `AssistantSettings` (одна строка на
пользователя, `persona` строкой, дефолт `butler`), миграция
`021_create_assistant_settings`. Характер-листы четырёх персонажей
(🎩 дворецкий/🏋️ тренер/💼 директор/📊 финансист) — единая точка сборки
в `app/assistant/personas.py::build_insight_prompt`, вставляется в
system-промпт в пяти местах: `app/scheduler/briefing.py`,
`evening_reflection.py`, `finance_report.py`, `weekly_digest.py`, и
новый `app/conversation/chat_reply.py`.

**Разговорный фолбэк — новое, не было раньше.** `Intent.CHAT`
(`app/conversation/parser.py::_looks_like_chat`) заводится, если
сообщение оканчивается на «?» либо начинается с приветствия/
благодарности («привет», «спасибо», «как дела» и т.п.) — самый низкий
приоритет во всём waterfall парсера, ничего из уже распознаваемых
команд не перехватывает (проверяется последним, перед дефолтным
ADD_TASK). До этой фичи такое сообщение молча становилось задачей с
этим текстом как заголовком — теперь персонаж отвечает по существу,
используя ограниченное окно контекста (последние 10 записей Memory —
без роста стоимости AI-вызова со временем использования, обсуждали с
владельцем отдельно). Без AI-клиента или без персонажа (например,
старые тесты собирают движок без них) — тихий откат на прежнее
поведение (текст становится задачей), сообщение никогда не теряется.

Переключатель — `/ui` → «Настройки» → «Персонаж» (не в боте, явное
решение владельца). REST API `app/api/assistant.py`
(`GET`/`PUT /assistant/persona`).

**Осознанно отложено на Фазу 2** (см. спеку, «Что НЕ входит»): более
тесная память диалога (сейчас — только записи Memory, не сырые реплики
за последние дни), редкие незапланированные сообщения персонажа вне
расписания, собственные имена персонажей.

1056 тестов (+30 к концу ночи #3), ruff/black чисты. Деплой прошёл
чисто (миграция применилась, контейнеры перезапустились), `/health` и
`/assistant/persona` (401 без токена — роут на месте) проверены сразу
после. **Живой проверки в реальном Telegram/на `/ui` НЕ было** — см.
п.0 в «Что сделать в начале новой сессии».

### 7. Два независимых мелких улучшения (коммиты `6a8117f6`, `4f77c8b9`)

Без запроса конкретной фичи — владелец попросил «что-то, что не
требует моего контроля»:

- **«Ещё N не поместилось» у всех списков.** У задач и дневника это
  уже показывалось (см. AUDIT.md, B-10 из старой практики), у
  привычек/полки/целей/людей/настроения — нет: сводка показывала
  ИСТИННОЕ общее число («12 привычек»), но кнопок с действием было
  только 10, расхождение не объяснялось. У финансов общего числа не
  было видно вообще. Общий хелпер `_hidden_note` в `keyboards.py`,
  переиспользован и в `_build_tasks_summary` (было продублировано
  вручную). 5 новых тестов, 1061 всего.
- **Бейджи закрытых тем подключены** (`badge-100/300/600/1000.png`,
  все 4 скина) — были сгенерированы раньше, не подключены нигде.
  Заодно поймана настоящая CSS-специфичность (не на глаз, через
  `getComputedStyle`): общий `:root[data-skin=X] button { background:
  ... }` (шорхэнд) побеждал более специфичное по классам, но менее
  специфичное по элементам правило на `background-size` — картинка
  подключалась, но рисовалась не на весь кружок. Детали — коммит
  `4f77c8b9`. **Аватар (`avatar.png`) сознательно НЕ подключён** — см.
  «Что осталось», ниже, там же ссылка на `designs/004-ui-layout.md`.
  Проверено локальным `http.server` во всех 4 скинах + no-skin случае
  до деплоя (Browser pane внутри агента, `getComputedStyle`), после
  деплоя — HTTP 200 на все 16 файлов на самом проде. **Визуально на
  проде глазами не смотрено** — только вычислительно.

---

## Что сделано в прошлой сессии (23.08, ночь #3)

### 1. Аудит кода — 10 находок, все исправлены и задеплоены (коммит `83eaac2`)

Полный проход по `app/` (5 параллельных ревьюеров + ручная проверка
каждой находки), поверх `AUDIT.md`/14–16.08 — тот аудит по-прежнему
держится. Новое:

- `TaskService.mark_reminded` не проверял владельца задачи — добавлен
  `telegram_user_id`/`owned_or_none`, как у всех остальных id-based
  методов.
- `send_task_reminders_job` слал напоминания без фильтра владельца (у
  привычек фильтр уже был) — добавлен.
- `RewardsService.claim_today`: гонка check-then-act, двойной клик
  «Забрать» ловил необработанный `IntegrityError` — теперь откатывается
  и ведёт себя идемпотентно.
- `weekly_digest.py` и напоминание о привычке слали заголовки/AI-инсайт
  в HTML-сообщении без экранирования — добавлен `_esc()`.
- Ответ на кнопку «Добавить задачу» мог перехватываться другим
  намерением («готово …» → COMPLETE_TASK) — добавлена `parse_add_task`.
- Удалены `_open_journal_prompt` (мёртвая) и 3 метода-сироты
  `HabitService`, оставшиеся после bulk-фикса стриков.
- 6 REST-роутеров переведены на `build_X_service` из `core/container.py`
  вместо повторной сборки репозитория+сервиса.
- `get_drive_client` теперь кэшируется на процесс, как `get_ai_client`.

867 тестов на этом шаге, ruff/black чисты.

### 2. Диагностика удержания → Engagement Hooks (коммит `f10b5cd4`, `specs/016-engagement-hooks.md`)

Владелец сказал прямым текстом: реально пользуется только дайджестами
каналов, задачи/цели/календарь не заполняет, утренний
брифинг/рефлексию/чек-ины «иногда читаю, но не действую». Разобрал
push-сообщения по коду: 4 из 6 ежедневных слотов ждут напечатанный
текстовый ответ, и ни одно действие по кнопке нигде не вознаграждается
— rewards существовал только как отдельный поход в `/ui` + клик
«Забрать».

Сделано (без новых доменов — только перенастройка существующего):

- **`RewardsStatus.just_claimed`** — `claim_today` различает «первый раз
  за сегодня» от повторного вызова в тот же день.
- **Награда за любое «сделал»-действие**, не только отдельный визит в
  `/ui`: по кнопкам — задача выполнена, привычка отмечена, прогресс цели
  вырос/цель завершена, пункт полки просмотрен
  (`app/telegram/callbacks.py::_reward_line`); через чат — создание
  задачи, запись в дневник, ответ на проактивный вопрос
  (`ConversationEngine.rewards_service`, опционален, как `ai_client`).
  Показывается «🪙 +N · 🔥 стрик» в том же сообщении, один раз в день.
  Откат прогресса цели (-10%) и удаления — не награждаются.
- **Утренняя рефлексия (была отдельной джобой в 10:30) слита в
  `send_morning_briefing_job`** — тем же паттерном, что вечерний чек-ин
  уже использует для gap-вопроса. Минус один push-слот в день;
  `proactive_prompt_morning_hour/_minute` убраны как dead config.
  **Важно**: реальный эффект — вопрос теперь звучит во время
  `morning_briefing_hour` (по умолчанию 8:00), а не в 10:30 — это
  оказалось дальше друг от друга, чем казалось на этапе спеки. Если
  8:00 не подходит под настроение вопроса — поменять
  `morning_briefing_hour`/`_minute` в `.env`.
- **Дайджест каналов: кнопка «⭐ Сохранить»** под сообщением — создаёт
  запись в дневник, снимается после тапа. Изначально планировалась
  кнопка под каждым ПОСТОМ, но `DigestService.build_digest_text` уже на
  сборке сплющивает посты в один текст без id — пришлось урезать до
  «сохранить весь дайджест целиком» (см. спеку, раздел пересмотрен по
  ходу реализации).

Сознательно не сделано в этот заход (см. спеку, раздел «Что НЕ входит»):
кнопки вместо свободного текста для самой рефлексии/gap-вопросов
(вопросы открытые по смыслу, сужать их до кнопок — не то же самое, что
снимать трение), и метрика открыл/нажал по каждому push (если этой
итерации окажется мало — отдельная спека).

886 тестов на этом шаге, ruff/black чисты.

### 3. Финансы (коммит `8b51cbf2`, `specs/017-finance.md`)

Владелец вернулся к финансовой аналитике тем же вечером («делай по
максимуму, можешь потом и финансы делать»). Реализованы все три
решения владельца из прошлых сессий, без пересмотра:

- **Шестой домен** `app/finance/` по тому же шаблону, что
  tasks/habits/goals/watchlist/memory: `Transaction` (kind
  income/expense, category, amount, note, occurred_at),
  `FinanceRepository`, `FinanceService.add_transaction`/
  `build_period_summary`. Категории и то, какие из них «обязательные»
  (`rent`/`utilities`/`subscriptions`/`credit`) — фиксированы в коде
  (`app/finance/models.py`), не отдельная таблица.
- **Разбор в чате** — новый модуль `app/conversation/amount_parser.py`
  (по образцу `date_parser.py`): «1200», «1.5к», «2 тысячи», с/без
  «руб»/«р»/«₽». Новые intent'ы `ADD_EXPENSE`/`ADD_INCOME`, триггеры
  («потратил», «купил», «оплатил», «получил», «зарплата», ...) —
  **намеренно проверяются раньше watchlist**, не после: иначе
  «потратил 500 на фильм» перехватывался бы голым триггером watchlist
  «фильм». Расплата (осознанно принята, см. комментарий в parser.py):
  «купил книгу для дочки» без суммы теперь тоже уходит в ADD_EXPENSE
  (переспросит сумму), а не в watchlist.
- **Награда** (specs/016) — на успешных ADD_EXPENSE/ADD_INCOME с
  распознанной суммой, тот же `_with_reward`, что и у остальных.
- **Отчёт** — `app/scheduler/finance_report.py` +
  `send_finance_report_job`, отдельная джоба (не часть `app/digest/`,
  как решено), по воскресеньям, считает за ТЕКУЩИЙ календарный месяц
  (не скользящее окно недели — доход/аренда естественно месячные), но
  отправляется еженедельно как промежуточная сверка. AI-фраза поверх
  готовых чисел, тихий фолбэк на сухой отчёт.
- Миграция `018_create_transactions` — прогнана на проде при деплое,
  без проблем (SQLite для локальной scratch-проверки не подошла — вся
  цепочка миграций проекта требует Postgres из-за старых ALTER
  CONSTRAINT; Docker на этой машине не установлен вообще, см.
  «Технические нюансы» ниже).

**Изначально REST API и `/ui` были сознательно отложены** («сначала
посмотреть, приживётся ли ввод текстом») — но владелец в тот же вечер
не нашёл кнопок и попросил «полный арсенал, и на сайт тоже». Добавлено
следующим коммитом (`77978855`), без пересмотра остального:

- **Бот**: шестой пункт меню «💰 Финансы» → экран раздела → список за
  месяц с итогами/нормами → две кнопки «добавить» (трата `f|n`, доход
  `f|i` — у финансов их два, не один) → удаление (`f|x|{id}`, без
  награды). `parse_finance_expense`/`parse_finance_income`
  (`app/conversation/parser.py`) — по образцу `parse_add_task`, ответ
  на кнопку без слова-триггера.
- **REST API** (`app/api/finance.py`): CRUD транзакций + `/finance/summary`,
  тонкий слой поверх `FinanceService`, как у остальных доменов.
- **`/ui`**: седьмая вкладка «Финансы», карточка со сводкой месяца,
  разбивкой по категориям (сверх нормы — токен `--overdue`, как у
  просроченных задач) и формой добавления. Иконка — существующий глиф
  `coins`, новый SVG заводить не пришлось.
- Проверено вручную: REST API — end-to-end поверх `ASGITransport`
  (create→list→summary→delete→404); `/ui`-карточка — в браузере через
  `file://` с подменённым `api()` (pending, что `localStorage`
  недоступен в `data:`-песочнице превью — сам рендер и JS-логика
  проверены полностью).

**Что осталось отложенным даже после этого** (см. спеку, «Что НЕ
входит») — уже по архитектурным, не UX-причинам: разбор даты в
сообщении о трате (`occurred_at` всегда «сейчас»), автообнаружение
периодических платежей, копейки/мультивалютность, изменение/удаление
транзакции через чат (только через кнопку/API).

959 тестов (+73 к началу этого пункта), ruff/black чисты.

### 4. Личный CRM + трекер настроения (коммит `fec91fc`, `specs/018-personal-crm.md`, `specs/019-mood-tracker.md`)

Последние два пункта из «Дальше по списку» прошлой версии файла (третий,
фокус-сессии, не начат — см. «Что сделать в начале новой сессии»).
Оба сделаны СРАЗУ с REST API и `/ui`-карточкой в одном коммите, не
отложены на «сначала посмотреть, приживётся ли» — прямой урок из истории
с финансами выше (владелец не заметил кнопки без API/`/ui` и попросил
«полный арсенал» в тот же вечер).

**Личный CRM** — седьмой домен `app/crm/` (`Contact`: имя, день
рождения БЕЗ ГОДА — только месяц+день, повторяющаяся дата для нэджей, не
возраст). Добавление — только кнопкой (`app/telegram/pending_input.py`,
`CONTACT_ADD`) + новый `app/conversation/birthday_parser.py` («Аня
14.09» → имя + дата), не через общий `parse_intent` waterfall: у
контакта, в отличие от трат, нет естественной глагольной формы,
безопасной для перехвата в свободном тексте где угодно. Два новых нэджа
в `app/scheduler/nudges.py` (файл расширен, не переписан — сигнатура
`build_nudges` получила пятый опциональный параметр `contact_service`,
старые вызовы не тронуты): «давно не писал(а)» на 30-й день после
последнего контакта, «день рождения скоро» на порогах 3/1/0 дней — тот
же приём, что у дедлайна цели. Бот: пункт меню «📇 Люди» (домен-буква
`c`). REST API `app/api/crm.py`. Карточка на `/ui` — грид-CSS сразу с
правилом `grid-column: span 3`, тот же баг, что чинили для финансов
(см. шапку файла), в этот раз не повторён.

**Трекер настроения** — восьмой домен `app/mood/` (`MoodEntry`: оценка
1-5, момент, заметка — заметка не заполняется кнопкой в этой итерации).
Daylio-style: один тап по эмодзи (😢😕😐🙂😄), без формы, без
обязательного текста, несколько отметок в день — не ошибка. Точка входа
№1 — 5 кнопок ПОД вечерним дневниковым AI-вопросом (21:00,
`send_evening_reflection_job`): тап дописывает подтверждение в то же
сообщение и снимает клавиатуру, тот же приём, что у «⭐ Сохранить» под
дайджестом (не заводили отдельный push-слот). Точка входа №2 — пункт
меню «😊 Настроение» (домен-буква `m`) — тот же набор кнопок плюс
история, доступно в любой момент дня, не только вечером. Третий
подграфик в `app/scheduler/charts.py` (точки настроения за 30 дней,
НЕ линия — несколько отметок в день не должны читаться как единый
тренд) — `gather_chart_data`/`render_chart` получили опциональный
`mood_service`, обратная совместимость сохранена. REST API
`app/api/mood.py`. Карточка на `/ui` — 5 кнопок сверху, тот же принцип
«один тап», что и в боте.

Награда (specs/016) — на «написал(а)» и на отметку настроения (оба
«сделал»-действия), не на удаление ни там, ни там — тот же принцип, что
у финансов/задач/привычек.

**Проверено:** REST API обоих доменов — end-to-end НА САМОМ ПРОДЕ
(`curl` с тестовым `telegram_user_id=999999999`, create→list→
update/mark→delete→404, тестовые записи удалены за собой) — сильнее
предыдущей проверки на finance (там был только `ASGITransport`).
Живая проверка бота/`/ui` глазами владельца — НЕ сделана, см. «Что
сделать в начале новой сессии».

`/ui`-карточки визуально не проверены агент-браузером (сессия
`agent-browser` зависла на установке Chrome в фоне и не завершилась за
время сессии, вторая попытка не предпринималась) — вместо этого
статическая проверка: JS-синтаксис извлечённого `<script>`-блока через
`node --check`, баланс открывающих/закрывающих `<section>`. Разметка и
JS написаны по образцу существующих карточек (finance/habits), риск
визуального дефекта невысокий, но не исключён — стоит открыть `/ui` на
проде и посмотреть глазами при следующей возможности.

1026 тестов (+67 к началу этого пункта), ruff/black чисты.

### 5. Картинки скинов — 29/30 подключены (коммит `4b08c8a`)

Владелец сгенерировал все 30 картинок из `designs/002` вечером 23.08
(папка `Стили/` рядом с репозиторием, не в гите — попутно найдена и
починена причина, почему она не игнорировалась, см. п.3 в «Технических
нюансах» ниже). Обработка и подключение — в этой же сессии, сразу после
готовности.

**Обработка.** `pngquant` на машине не установлен (`winget` не нашёл
пакет) — написан `lifeos-agent/scripts/process_skin_images.py`:
Pillow, `Image.thumbnail` (ресайз с сохранением пропорций под целевой
размер из старой таблицы 002) + `Image.quantize(method=FASTOCTREE)`
(сжатие палитры — RGBA поддерживает только `FASTOCTREE`/
`LIBIMAGEQUANT` из методов Pillow, второй не скомпилирован в этой
сборке). Для `pixel` — `NEAREST` вместо `LANCZOS` при ресайзе (иначе
размывает резкие пиксельные грани) и палитра поменьше (48 цветов
против 128 у остальных). Итог: graffiti 408К/13 файлов, pixel 88К/13,
groovy/japan +2 файла каждый (финансы). Качество и прозрачность
проверены: alpha-канал программно (`getchannel("A").getextrema()`, не
на глаз — разные вьюеры рисуют прозрачность по-разному), несколько
картинок каждого скина просмотрены напрямую и на реальном
`card-bg`-фоне (`Image.alpha_composite`) — читаются чётко, halftone и
контуры не теряются на тёмном graffiti-фоне.

**Подключение в CSS.** graffiti и pixel были в состоянии «слот готов,
картинки нет, специально не подключено, чтобы не открыть пустую дыру»
(так и было закомментировано в `index.html` с 21.08/22.08) — теперь
включены штатным паттерном: `#taskList .empty` / `#habitList .empty` /
`#goalList .empty` / `#shelfMovieToWatch .empty` с background-image,
`[data-domain="rewards"]` с `motivation.png`, `.brand::before` с
`logo.png` (pixel — включено; graffiti — НЕТ, см. ниже). Плюс у всех
четырёх скинов (включая groovy/japan) — картинка для карточки
«Финансы» (её не было в исходном наборе 002, дополнена 23.08 днём,
после того как появился сам домен): `#financeList .empty` для пустого
списка транзакций и **компактный угловой герой** (не через всю
карточку, как у weekly/goals — там форма занимает почти всё место,
крупная фоновая картинка легла бы поверх неё).

**graffiti/logo.png НЕ подключён.** Сгенерировался с текстом
`LIFEOS TRACKER` вместо одного слова `LIFEOS` — прямое нарушение
промпта (`No other text besides the word LIFEOS`). Файл удалён из
`skins/graffiti/` до перегенерации, `designs/002` переписан под
единственный оставшийся промпт (тот же, что уже был, без изменений в
тексте). Скин graffiti пока показывает кольцо `.brand-mark` вместо
своего леттеринга.

**Проверено:** HTTP 200 на все 25 подключённых файлов (fetch HEAD из
браузера) + `getComputedStyle(...).backgroundImage` совпадает с
ожидаемым путём для каждого из четырёх скинов — сначала на локальном
`python -m http.server` в `app/web/static/`, затем то же самое ещё раз
на самом проде после деплоя. **Полноценный скриншот не снят** —
`computer.screenshot` весь вечер отвечал «Browser pane is not
displayed» (клиент не открывал панель), `agent-browser` (CLI) зависал
на установке Chrome в фоне и был остановлен без результата. Первое,
что стоит сделать в начале следующей сессии — посмотреть на `/ui`
глазами (см. п.1 в разделе «Что сделать»).

*Побочная находка при отладке проверки:* при живой JS-мутации
`data-skin` на невидимой (не в фокусе клиента) вкладке
`getComputedStyle` иногда возвращал устаревшее значение для одного
конкретного свойства, пока остальные уже обновились — воспроизведено,
не баг в CSS (перепроверено полной перезагрузкой страницы на каждый
скин, всё сошлось). Если проверка через JS в браузере покажет
«странное» значение — сначала `location.reload()`, а не сразу чинить
CSS.

---

## Технические нюансы, добавившиеся за сессию

1. **`ConversationEngine`/`RewardsService` — опциональность как паттерн.**
   Пятый опциональный сервис в конструкторе движка (после `ai_client`,
   `goal_service`, `pending_prompt_service`, `watchlist_service`) — везде
   один и тот же приём: `None` тихо отключает фичу, старый код и тесты
   без изменений.
2. **`send_digests_job` теперь несёт `(digest.id, text)` пары, а не
   плоский список текстов** — кнопке нужен id темы в `callback_data`.
   Источник текста для «⭐ Сохранить» — не повторный запрос к
   `DigestService` (он расходует «новые посты», второй вызов вернёт
   другое или ничего), а `query.message.text` — то самое сообщение, на
   котором висит кнопка.
3. **Прод-папка `пороли чтоле/` (хранилище секретов) и папка исходников
   картинок для скинов больше НЕ мешают `deploy.sh`** — решено насовсем
   в этой сессии, не временным переносом в scratchpad, как раньше.
   Причина находки: во время этой сессии рядом появилась новая
   untracked-папка `Стили/` (владелец генерил в неё картинки параллельно)
   — и выяснилось, что запись `.gitignore` под неё существовала, но с
   ДРУГИМ именем («Файлы для дизайна стилей/» вместо реального «Стили/»)
   — то есть не работала вообще, и `git status --porcelain` (на нём
   стоит проверка чистоты в `deploy.sh`) всё это время видел лишнюю
   untracked-директорию. Добавлены оба варианта имени плюс
   `пороли чтоле/` — все три теперь по-настоящему игнорируются, коммит
   `41db043`. Если появится новая подобная папка с ещё одним именем —
   тот же приём (добавить в `.gitignore`), а не перенос/`git stash`.
4. **Docker не установлен на этой машине вообще** (ни в Bash, ни в
   PowerShell) — «scratch-проверка миграций» из старых граблей теперь
   означает только SQLAlchemy-level проверку модели
   (`Base.metadata.create_all` на временном SQLite) плюс структурную
   проверку графа ревизий Alembic (`ScriptDirectory.walk_revisions`), а
   не реальный прогон SQL. Полная цепочка миграций проекта не
   прогоняется на SQLite целиком — старые ревизии используют
   `ALTER CONSTRAINT`, которого SQLite не поддерживает (падает на
   007-й). Единственная точка, где новая миграция реально проверяется
   на настоящем Postgres — сам `deploy.sh` на проде; он безопасен (не
   перезапускает контейнеры при сбое миграции), но это значит первый
   реальный прогон новой миграции — уже в проде, не раньше.
5. Старые грабли (деплой только через `scripts/deploy.sh`,
   `pydantic-settings extra="forbid"`→`ignore`, `.claude/settings*.json`
   коммитит только владелец вручную, PATH для Node/Python не
   подхватывается в старой Bash-сессии) — без изменений.

---

## Состояние проекта

```bash
cd lifeos-agent
python -m pytest -q                                    # 1056 тестов
python -m ruff check app tests migrations scripts
python -m black --check app tests migrations scripts
bash scripts/deploy.sh          # деплой (только так)
bash scripts/deploy.sh --check  # сверка прод/гит/локаль
```

Новое в структуре: `specs/016-engagement-hooks.md`,
`specs/017-finance.md`, `specs/018-personal-crm.md`,
`specs/019-mood-tracker.md`, `specs/020-butler-personas.md`,
`app/finance/`, `app/crm/`, `app/mood/`, `app/assistant/` (четыре новых
домена: models/repository/service/schemas, у assistant ещё
`personas.py` — характер-листы), `app/api/finance.py`, `app/api/crm.py`,
`app/api/mood.py`, `app/api/assistant.py`,
`app/conversation/amount_parser.py`, `app/conversation/birthday_parser.py`,
`app/conversation/chat_reply.py`, `app/scheduler/finance_report.py`,
`migrations/versions/018_create_transactions.py`,
`migrations/versions/019_create_contacts.py`,
`migrations/versions/020_create_mood_entries.py`,
`migrations/versions/021_create_assistant_settings.py`,
`tests/telegram/test_jobs.py` (раньше `app/telegram/jobs.py` не был
покрыт тестами вообще — теперь частично, только то, что тронуто за
сессию), `tests/finance/`, `tests/crm/`, `tests/mood/`,
`tests/assistant/`, `tests/conversation/test_chat_reply.py`,
`tests/conversation/test_engine_chat.py`,
`scripts/process_skin_images.py` (замена `pngquant`, которого нет на
машине — см. раздел 5 выше).
`app/web/static/index.html` — восьмая и девятая вкладки/карточки
(«Люди», «Настроение»), полные наборы иллюстраций у graffiti/pixel,
картинка на карточке «Финансы» у всех четырёх скинов — без новых
файлов (один большой HTML, как и раньше). `app/web/static/skins/{graffiti,
pixel}/` — новые (были пустыми папками), `skins/{groovy,japan}/` — по
2 новых файла (финансы).

---

## Что осталось

**Ближайшее и блокирующее:** живьём проверить персонажей (переключатель
на `/ui` + голос в реальных сообщениях бота, см. п.0 в «Что сделать») —
ни разу не увидено. Плюс одна картинка — `graffiti/logo.png`
(перегенерировать без лишнего текста, см. шапку файла), и живьём
посмотреть на `/ui` во всех четырёх скинах — ни разу не снят скриншот
(см. п.1 в «Что сделать»).

**Не подтверждено живым использованием:** Персонажи бота (Фаза 1,
специфика — раздел 6 «Что сделано»), Engagement Hooks, Финансы, личный
CRM, трекер настроения (включая все кнопки/API/`/ui`) и картинки
graffiti/pixel/финансов — всё зелёное и задеплоено (у CRM/mood и
персонажей ещё и REST API/роут проверен на самом проде, у картинок —
HTTP 200 + computed CSS тоже на проде), но владелец не видел ни одно из
этого глазами в реальном Telegram-чате/браузере.

**Фаза 2 персонажей** (не начата, отдельная спека при необходимости,
см. `specs/020-butler-personas.md`): более тесная память диалога (не
только записи Memory), редкие незапланированные сообщения персонажа,
собственные имена.

**Дальше по списку:** фокус-сессии/Pomodoro (см. «Что сделать в начале
новой сессии», п.3) — последнее из трёх, единственное, где владелец сам
предупреждал о неопределённости масштаба (стоит уточнить перед стартом).
Также: метрика открыл/нажал по push-сообщениям, если engagement-hooks
окажется недостаточно.

**Отложено осознанно:** перетаскивание плиток интерфейса, голосовой
ввод (до решения по монетизации и счётчика минут), аватар у всех
четырёх скинов (`avatar.png` сгенерирован, НЕ подключён — сознательно:
`designs/004-ui-layout.md` фиксирует отказ владельца от блока
профиля-аватара в шапке, «ещё один блок ради одного изображения»; если
идея вернётся — не в шапку, искать другое место). **Бейджи порогов
подключены** в эту сессию (коммит `4f77c8b9`) — theme picker уже
существовал, это не новый блок, только его закрытые темы теперь
показывают картинку вместо цвета+числа.

**Из `AUDIT.md`:** P-3 (pgvector, после ~1000 записей памяти) — не
срочно. Проверено в этой сессии: B-7/B-8 (дата+год/дата+время) на
самом деле уже закрыты коммитами от 15.08, несмотря на старую пометку
здесь — сам `AUDIT.md` это подтверждает в статус-блоке. B-6 (границы
слов в парсере) — принятый компромисс ADR-004, не открытый баг (только
1 из 4 примеров в `AUDIT.md` реально лечится якорем, остальные
потребовали бы семантического разбора — решили не делать).

**Из старых идей:** рецепты по фото продуктов, Weekly Review как
диалог, проверить отмену пробного VPS SmartApe. Режимы фокуса — см.
раздел 4 выше, теперь с конкретным конкурентным контекстом (Reclaim/
Motion), но реалистичный масштаб для Telegram-бота намного тоньше.

---

# Архив прошлых сессий (сжато 23.08 — экономия токенов)

Полный построчный нарратив по каждой сессии до 21.08 удалён из этого
файла ради размера — при вставке в чат он стоил токенов больше, чем
давал пользы. Ниже — сухой остаток: решения и факты, которые всё ещё
могут понадобиться. Если нужны подробности конкретной правки — смотреть
`git log`, коммиты подписаны содержательно.

## Хронология фич (кратко, по сессиям)

**16.08 — аудит безопасности + инфраструктура.** Закрыты все дыры из
`AUDIT.md`: Postgres/API были доступны из интернета (порты на
`127.0.0.1`), REST API не имел аутентификации (добавлен `X-API-Token`,
пустой токен закрывает доступ, а не открывает), токен бота утекал в
логи (перевыпущен), бот отвечал кому угодно (добавлен owner-фильтр).
Ежедневные бэкапы БД на Google Drive (`scripts/backup_db.sh`, крон
03:00 Amsterdam, ротация 7 дней локально / 14 на Диске, проверено
восстановлением). Деплой переведён на `scripts/deploy.sh` из гита —
старый способ (`tar` + ручной `docker compose up` по ssh) запрещён,
это и был источник рассинхрона копий кода. Живые баги с реального
использования: команды проваливались в дневниковый перехват, время
дедлайнов съезжало на 3 часа (UTC не переводился в местное), «напомни»
всегда уходило в поиск по памяти вместо создания задачи, дата-парсер
не понимал разговорные форматы времени. Производительность: N+1 по
стрикам привычек исправлен, поиск переведён на `ILIKE` в БД, один
`httpx.AsyncClient` на процесс, добавлены индексы. Побочная находка:
SQLite в тестах не сравнивает кириллицу регистронезависимо (только
Postgres на проде это умеет) — `tests/support.py::sqlite_engine()`
чинит это для тестового окружения.

**17.08 (вечер) — дайджест каналов + меню + голос выключен.** Дайджест
Telegram-каналов реализован без юзербота (скрейпинг `t.me/s/<channel>`
через `beautifulsoup4`). Меню бота переведено на единый паттерн «кнопка
домена = экран раздела со списком + добавить» (`specs/014`).
**Голосовой ввод выключен намеренно** — единственная статья расходов
на AI с заметной ценой (~$1-2/пользователь/мес), решение оставить его
выключенным до модели монетизации живёт до сих пор (см. шапку файла).
Написан `MULTIUSER.md` — разбор перехода к продукту: Google Drive в
текущем виде **не годится** для мультиарендности (refresh-токены живут
7 дней в статусе Testing), только Telegram Stars для платежей, 152-ФЗ
требует данных россиян физически в РФ (сервер в Нидерландах — открытая
архитектурная проблема), `/ui` уязвим через `telegram_user_id` в
query-параметре (лечится Mini App).

**17.08 (ночь) — TMDb/Google Books, календарь, редизайн.** Карточки
книг/фильмов с обложками: TMDb недоступен из России напрямую (нужен
свой прокси `app/api/poster.py`), Google Books требует параметр
`country` (без него 503 на любой запрос). Календарь на `/ui` с панелью
часов. Все карточки по умолчанию свёрнуты (состояние в localStorage).
Редизайн бота и `/ui`: даты словами, цветные метки срочности, мини-игра
«Ежедневный визит» (`app/rewards/`) — важно: `service.py` обращается к
`coins.py` **через модуль** (`from app.rewards import coins`), не
прямым импортом имени функции, иначе тесты патчат две разные ссылки.

**18.08 — REST API дайджестов, иконки, груви, японский.** Дайджесты
получили полноценный REST API (`app/api/digest.py`) и карточку на
сайте. Базовый набор иконок переработан (глифы рисовались под 24px,
жили на 12px — отсюда нечитаемость). Раскладка `/ui` пересобрана на
общую для всех скинов мозаику (`designs/004-ui-layout.md`) — до этого
каждый скин чинил композицию по-своему, что и было ошибкой. Скин
`groovy` переписан с нуля по дизайн-системе `designs/003` (первая
версия была просто перекраской токенов — критика была справедливой).
Скин `japan` сделан **старым способом** (переопределение токенов) —
это осознанно признано ошибкой, но не переделано; образцом для новых
скинов брать `groovy`. Написаны промпты для генерации иллюстраций
(`designs/002-skin-illustration-prompts.md`). Сжатие картинок:
`pngquant`, для groovy — палитра 200 / масштаб 1.0 → ~450КБ на скин
(24 картинки), для japan — палитра 64 / масштаб 0.75 → 228КБ (вес
там держит мелкая гравюрная штриховка, не палитра). Ассеты скина
грузятся лениво, только при выборе. Найденные и исправленные грабли
специфичности CSS (3 случая: `.empty` проигрывал `ul.item-list li`,
`[data-domain]` проигрывал `.grid > .card`, скругление календаря
наследовалось не то) и псевдоэлемент с `z-index` перекрывал заголовки
шести карточек.

**21.08 (утро) — новая машина, фикс `Settings`, 5 design-скилов.**
Настройка машины по `SETUP.md` (Python, Node.js, `pull_secrets.sh`).
Найден и исправлен баг: `pydantic-settings` по умолчанию запрещает
незнакомые ключи при чтении `.env` целиком (`POSTGRES_PASSWORD` нужен
только docker-compose) — фикс `extra="ignore"` в `Settings.model_config`.
Установлены 5 design-инструментов в `.claude/skills/`: `cast`/`paint`
(genjutsu), `animate`, `web-design-guidelines`, `impeccable` (хук на
каждую правку UI-файла в `.claude/settings.local.json`),
`agent-browser` (CLI на Rust — решил старую проблему со скриншотами).

**21.08 (вечер) — groovy по 003, graffiti с нуля, pixel-слоты.**
`agent-browser screenshot` заработал и на `file://`, и на прод-URL —
визуальная проверка скинов пошла по скриншотам, не только по
JS-геометрии. `groovy` доведён до `designs/003-groovy-design-system.md`
(радиусы карточек/полей/кнопок, крупное число монет, контраст
`--domain-text` на доменных кнопках, активная вкладка в шапке,
растяжение `decor-hill.png`); два пункта документа сознательно не
применены и задокументированы как обоснованный откат (заголовок карточки
19px не 26px — обрезается об угловой декор; декор внутри карточки, а не
за краем — прошлая попытка дала «наклейки» на чёрном фоне). `pixel`:
4 пустых состояния + мотивация подключены тем же паттерном, слоты под
картинки готовы, но не включены активно (без самих картинок — открытая
дыра). `graffiti`: CSS с нуля — единственный скин, переопределяющий
режим целиком (тёмные карточки, светлый текст), проверен агент-браузером
в dark и light. Итог сессии — 33 картинки (промпты в
`designs/002-skin-illustration-prompts.md`) остались единственным
блокером показа обоих новых скинов.

## Философия и архитектура (не менялись с 16.08)

**LifeOS Agent** — персональный AI Chief of Staff в Telegram,
single-user (не multi-tenant). Не чат-бот и не менеджер задач — цель
снять когнитивную нагрузку: бот сам держит контекст и проактивно
напоминает, а не только отвечает. Подробно — `PROJECT.md`.

ADR из `DECISIONS.md`: **ADR-001** модульный монолит, не микросервисы.
**ADR-002** LLM никогда не трогает БД напрямую, только через сервисный
слой. **ADR-003** spec-first — `specs/NNN` + `flows/NNN` до кода.
**ADR-004** простой код лучше LLM. **ADR-005** один сервис — одна
ответственность.

Слои: `API/Telegram → Service → Repository → DB`. `ConversationEngine`
Telegram-агностичен. Каждый домен — свои
`models.py`/`repository.py`/`service.py`/`schemas.py`; REST-роутеры
отдельно, в `app/api/`.

## Технический долг, который никуда не делся

Из `AUDIT.md`, архитектурный блок: **A-1** `telegram_user_id` не
проверяется как владелец сущности в сервисных методах, работающих по
`id`. **A-3** фабрика сервисов вместо ручной сборки в семи местах.
**A-4** `EngineResult` вместо парсинга ответа регуляркой
(`handlers.py::extract_created_task_title` — первая строка ответа о
созданной задаче намеренно не менялась, по ней регуляркой цепляются
быстрые кнопки, трогать без этого рефакторинга нельзя). Дедупликация
репозиториев (`BaseRepository[T]`), мёртвый код (`pick_and_open`,
`/memory/context`). **P-3** pgvector — не срочно, актуально после
~1000 записей памяти. **B-6/B-7/B-8** границы слов в парсере дат.

Из старых идей, не начато: рецепты по фото продуктов (Media Inbox),
персональные «режимы фокуса», Weekly Review как отдельный диалог,
проверить отмену пробного VPS SmartApe (Москва, старый заброшенный).

---

## Доступы, пароли, ссылки

### Репозиторий
[gezhenko95-jpg/LifeOS-Agent](https://github.com/gezhenko95-jpg/LifeOS-Agent),
ветка `main`. Локально: путь меняется от машины к машине (см. `SETUP.md`), код в
`lifeos-agent/`.

### Продакшн-сервер (Hostkey, Нидерланды)
- IP: `148.135.208.126`, домен: `lifeos-agent.ru`
- SSH: `ssh lifeos-eu` (алиас в `~/.ssh/config`, ключ
  `~/.ssh/lifeos_main`). Вход только по ключу.
- Проект на сервере: `/opt/lifeos/`.
- Caddy — реверс-прокси + HTTPS, `/etc/caddy/Caddyfile`, проксирует
  `lifeos-agent.ru` → `localhost:8000`.

### Домен и DNS (SmartApe)
- Аккаунт: `gezhenko95@gmail.com`, панель `cp.smartape.ru`.
- Домен `lifeos-agent.ru`, до 2027-08-14.
- DNS-записи — отдельная панель: **https://dns-01.smartape.ru:1501**,
  логин `user1764200`, пароль — **в менеджере паролей** (запись
  «SmartApe DNS»). В этот файл секреты не вписывать: HANDOFF целиком
  вставляется в чаты, а значит уходит наружу вместе с паролями.

### Старый VPS (SmartApe, Москва) — заброшен
IP `188.127.224.144`, алиас `lifeos-vps` — не использовать, с
российских VPS заблокирован `api.telegram.org`.

### Google Drive (Media Inbox)
`credentials.json` + `token.json` в `/opt/lifeos/`, не в git. Проект
`LifeOS Agent` (`lifeos-agent-505510`), scope `drive.file`.
Переавторизация — `scripts/drive_auth.py` (локально, не в контейнере).

### Полезные ссылки
- OpenRouter: https://openrouter.ai
- Hostkey: https://hostkey.ru
- SmartApe: https://www.smartape.ru
- Google Cloud Console: https://console.cloud.google.com

## Технические грабли (актуальны)

1. Windows + Bash-инструмент: `cd` в кириллический путь иногда
   спонтанно фейлится — повторить без `cd`.
2. SSH-ключ в веб-форме хостера — вставлять вручную одной строкой.
3. Postgres в Docker: `POSTGRES_PASSWORD` из `docker-compose.yml`
   применяется только при первой инициализации пустого volume.
4. `echo "x" >> .env` без финального переноса строки — значение
   приклеится к предыдущей строке.
5. JSON-колонки в SQLAlchemy: `JSON(none_as_null=True)`, если значение
   может быть `None` и по нему потом фильтруют `IS NULL`.
6. uvicorn за реверс-прокси: всегда `--proxy-headers
   --forwarded-allow-ips=*`.
7. Алембик: id ревизии ≤32 символов.

## Архитектура (не менялась)

Слои: `API/Telegram → Service → Repository → DB`. `ConversationEngine`
Telegram-агностичен. Каждый домен — свои
`models.py`/`repository.py`/`service.py`/`schemas.py`; REST-роутеры
отдельно, в `app/api/`.
