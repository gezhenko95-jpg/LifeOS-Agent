# LifeOS Agent — Handoff (2026-08-26, день #8)

Вставьте этот файл целиком в начало нового чата с Claude, чтобы
продолжить работу без потери контекста. Ниже раздела «более ранние
записи» — история, она не пересматривалась.

**Эта сессия шла на другой машине (Windows, тот же репозиторий) и
закончилась переносом на устройство владельца — начинайте с git pull,
не доверяйте локальному состоянию файлов.** Коммиты `afffccd`
(UI: фон groovy + graffiti-лого + унификация toggle*Row), `002f5e6`
(fix: TaskService везде через фабрику, A-3), `1bc2f4a` (docs: ROADMAP +
2 черновика спек) запушены в `origin/main`. **На проде НЕ задеплоено** —
только закоммичено, `bash scripts/deploy.sh` ещё не запускался в эту
сессию. Прод по-прежнему на `95ea45d`. 1303 теста, ruff/black чисты на
момент последнего коммита.

Сессия была не живой проверкой, а разбором приоритетного списка,
который владелец продиктовал одним сообщением (7 пунктов, "делаем"/
"пока пауза" по каждому) — часть закрыта кодом, часть уперлась в
правило `specs/README.md` («нет спеки — нет кода») и превратилась в
черновики спек с открытыми вопросами, один пункт (вёрстка Панели)
дошёл до выбора решения через `impeccable`-артефакт, но реализация
отложена по прямой просьбе владельца.

---

## Что сделать в начале новой сессии

**0. Синхронизация — обязательно первым шагом.** `git pull`, затем
свериться, что видно все три коммита выше. Если `.env`/секреты не
перенесены на это устройство — см. `SETUP.md`/`scripts/pull_secrets.sh`.

**1. Вёрстка «Панели» — решение принято, кода нет.** Причина бага
диагностирована и подтверждена: `align-items: stretch` на `.grid`
(`designs/004-ui-layout.md`, строка ~1852 в `index.html`) растягивает
все карточки ряда под самую высокую — Финансы с долгами+списком
операций тянут пустых соседей (реальный скриншот с прода это показал).
Три варианта показаны через `impeccable` на живых цифрах (артефакт
«Судьба Панели» — не хранится в репо, только в переписке чата).
**Владелец выбрал вариант C: потолок высоты (`max-height`) + внутренний
скролл на карточках `.grid` (не архитектурная правка — Панель остаётся
как есть).** Не реализовано, владелец прямо попросил не делать сразу —
начать с этого. Ключевые детали варианта:
- `max-height` + `overflow-y: auto` на карточках вкладки «Панель»
  (`.grid > .card`, не `.grid.single > .card` — там карточка одна и во
  всю ширину, ей это не нужно);
- нужно решить конкретное число `max-height` (высота соседней короткой
  карточки в типичном ряду? фиксированный px? см. мокап "C" в артефакте
  — там был скролл внутри `.txn`-списка, форма долга/кнопка оставались
  видны СНАРУЖИ скролл-области, не внутри неё — сохранить этот приём);
- touch-устройства: вложенный скролл внутри карточки может «съедать»
  вертикальный скролл страницы пальцем — проверить `touch-action` или
  хотя бы пронаблюдать на телефоне;
- затронет не только Финансы — любая карточка Панели, которая может
  разрастись (CRM с длинными заметками, дайджесты с много темами),
  получает тот же max-height не только точечно для Финансов.

**2. Персонажи фаза 2 — черновик спеки ждёт ответов, не код.**
`specs/027-butler-personas-phase2.md` — три независимых пункта (память
диалога/незапланированные сообщения/имена персонажей), каждый с
открытым вопросом владельцу (см. файл, раздел «Открытые вопросы»).
Без ответов реализация не начинается.

**3. Ферма+тамагочи+магазин — черновик спеки ждёт ответов, не код.**
`specs/028-farm-tamagotchi-rewards.md` — важный архитектурный факт
внутри: монеты сейчас (`app/rewards/coins.py::total_coins`) не хранимый
баланс, а производное число, пересчитываемое из истории чек-инов —
тратить физически нечем, магазину нужен ledger (`CoinTransaction`,
рекомендация зафиксирована в спеке). 4 открытых вопроса владельцу (что
продаёт магазин, что значит «неdecоративный» питомец, скорость роста
фермы, домен-буква бота).

**4. Задеплоить, когда будет готовность.** Три коммита выше в
`origin/main`, но НЕ на проде — `bash scripts/deploy.sh` с проверкой
`/health` после. Живая проверка фона groovy и graffiti-лого ещё не
делалась (я проверял в браузере через локальный статический сервер, не
на реальном `/ui` с прода) — стоит посмотреть глазами после деплоя.

**Более раннее, уже подтверждено владельцем и закрыто (без действий):**
вариант 3 UI на всех четырёх доменах (контакты/цели/долги живьём тоже
проверены, механика подтверждена), гвард закрытия задачи с подзадачами,
7 находок живой проверки + шкала настроения 1-7 из дня #7. Персонажи
бота фаза 1 (не фаза 2) — объяснено владельцу, не баг.

**Старый бэклог, всё ещё открыт:** доразрешение декора целей,
недостающие иконки финансов — ждут владельца (не код-задача).

---

## Что сделано в этой сессии (26.08, день #8)

Владелец продиктовал приоритетный список одним сообщением (пункты 1-12
из моего же предложения по важности), пометив каждый "делаем"/"дай
промт"/"пауза". Без живой проверки — session была про разбор бэклога,
не про баги на проде.

**Фон groovy, вариант 2 (`index.html`).** `.bg-blobs` (был контейнер
для двух блюр-кругов, уже выключен для groovy) переиспользован под
статичную сцену: decor-hill (полоса у низа экрана), decor-cloud-big,
decor-cloud-small, decor-stars — все уже лежали в `skins/groovy/`.
Первая попытка (один `background-image` с четырьмя `url()` через
запятую на одном селекторе) **не парсилась браузером** — правило
пропадало из `document.styleSheets` целиком без видимой причины
синтаксиса, воспроизвёл трижды, включая через `insertRule()`, который
показал тот же текст валидным в изоляции. Переписано на 4 независимых
слоя (два `<span>` контейнера + `::before`/`::after`) — тот же приём,
что уже используют `.card::before`/`::after` для углового декора
карточек. Заодно нашёлся реальный баг: `background-color: var(--accent)`/
`var(--ok)` от старых блобов-кругов не был сброшен и просвечивал
сплошным цветом сквозь прозрачные PNG — добавлен явный
`background-color: transparent`.

**graffiti/logo.png.** Владелец сгенерировал и прислал файл (промпт был
готов с прошлой сессии, см. `designs/002`). Альфа-канал проверен явно
через Pillow (`getbbox()`/`getextrema()` по углам и центру), а не на
глаз — на превью в чате фон выглядел белым, на деле полностью
прозрачный (0,0,0,0) по всем краям, это ожидаемо (см. правило приёмки в
`designs/002`: разные просмотрщики рисуют прозрачность по-разному).
Прогнан через существующий `scripts/process_skin_images.py` (720×200
box, `thumbnail()` вписывает с сохранением пропорций — реальный аспект
картинки ~1.78:1, итог 355×200). `.brand::before` подключён тем же
приёмом, что у groovy/japan/pixel, бокс 150×84 подобран под РЕАЛЬНЫЙ
аспект логотипа, не под черновой 720×200 канвас (тот же довод, что уже
был у japan: «лого здесь квадратный герб... поэтому и бокс другой
формы»). Живой скриншот через локальный статический сервер подтвердил
рендер на тёмном фоне.

**Унификация `toggle*Row()`.** Было 4 идентичные копии (задачи/
контакты/долги/цели, только Set и наличие `renderPanel` отличались) —
слиты в `toggleDomainRow(li, id, expandedSet, renderPanel)`, обёртки с
прежними именами не тронули ни один вызов на странице.

**Фабрика сервисов, A-3 (TaskService).** Восемь прямых сборок
`TaskService(TaskRepository(session))` в `jobs.py`/`handlers.py`/
`callbacks.py` (AUDIT.md называл их "существующим, не новым trade-off")
переведены на `build_task_service()` — задача теперь везде получает
проверку владения `contact_id`/`habit_id`/`goal_id`, не только в
`ConversationEngine`. Сознательно НЕ трогал остальные однострочные
сборки (`HabitService(HabitRepository(session))` и т.п.) —
`container.py` прямо документирует, что это не дублирование, фабрики
не для них.

**ROADMAP.md переписан.** Старый файл описывал Phase 1-5 с 12.08 и
разошёлся с реальностью (не упоминал 6 из 10 доменов, помечал
тестирование как не настроенное при 1303 тестах). Новый — по факту
сделанного, без предсказания будущего по фазам; живой бэклог по
приоритету по-прежнему только в этом файле.

**Специфицированы (не реализованы) два крупных пункта старого
бэклога.** `specs/README.md` требует спеку до кода — оба пункта были
только устными решениями в архиве HANDOFF, без файла. Написаны
`specs/027-butler-personas-phase2.md` и
`specs/028-farm-tamagotchi-rewards.md`, каждый с открытыми вопросами
владельцу вместо готового решения — см. п.2/3 выше.

**Вёрстка «Панели» — диагностирована, решение выбрано, не
реализовано.** Владелец показал реальный скриншот с прода (три
карточки в ряду, Финансы разрослись, соседи растянуты и показывают
пустоту). Причина найдена в CSS (`align-items: stretch`,
`designs/004`). Через `impeccable` показаны 3 варианта + вариант
владельца («убрать Панель вообще, вкладки с индикатором внимания») на
реальных цифрах Финансов (Ашан 800₽, кредит 20000₽, долг «Альфа»
26300/27000₽) — артефакт `panel-layout-options.html`, не сохранён в
репо. **Владелец выбрал C (max-height + скролл)**, прямо попросил не
делать сейчас — см. п.1 выше.

1303 теста (без изменений в количестве — рефакторинг и CSS без нового
покрытия), ruff/black чисты. Три коммита, см. вступление.

---

## Что сделано в прошлой сессии (25.08, день #7)

Владелец скидывал находки живой проверки по одной (скриншотами), явно
попросил не чинить до слова «готово» — весь разбор шёл по коду на месте
между находками, план заранее не согласовывался.

**Итоги недели — специфичность, а не логика.** `.segmented-btn` тонула
в том же классе бага, что уже чинили для `.tab.active` в прошлой сессии
(см. архив ниже): общий groovy/graffiti `button` (0,2,1) раздувал её до
44px-кнопки с паддингом 22px, три сегмента переставали помещаться в
карточку. Явный оверрайд — тот же приём, только применённый ещё раз.
Заодно график "Задачи" переведён с `.fa-bar` (язык Финансов) на
вертикальные столбики (язык соседнего "Настроение" в той же карточке) —
не должны выглядеть как два разных графика рядом.

**Дайджесты.** Название темы стало кнопкой-разворачивателем тела темы
(список каналов + форма добавления канала), свёрнуто по умолчанию —
тем может быть несколько.

**Настроение — три отдельных решения в одной фиче.** (1) `.mood-label`
был жёстко покрашен в `var(--muted)` — под groovy кнопки настроения
красятся в `--coral`, коричневатый muted на розовом проваливал контраст.
`color: inherit` решил это и заодно позволил (2) заменить эмодзи на свой
набор SVG-лиц (решение владельца через явный вопрос) — один
параметризованный силуэт (кривизна рта на уровень, брови/глаза только на
двух крайних), а не семь иллюстраций с нуля, `currentColor` следует за
тем же контрастным цветом, что уже подобран для текста кнопки в любом
скине. (3) Шкала расширена 1→7 (тоже решение владельца) — `MIN_SCORE`/
`MAX_SCORE` в одном месте (`app/mood/models.py`), бот (`keyboards.py`)
уже строил клавиатуру циклом по этим константам и расширился сам,
руками поправлены только захардкоженные "/5" в тексте и `SCORE_EMOJI`
(добавлены 2 новых эмодзи для бота — Telegram не рисует кастомные SVG).
matplotlib-график настроения (PNG в дайджест) — `set_ylim`/`set_yticks`
тоже по константам, не жёстко 1-5.

**Цели.** Ручной `<input type="number">` заменён на `<input
type="range">`, полоска/число обновляются вживую по `input`-событию,
сохранение — по кнопке ✓, как и раньше (осознанно не автосейв на каждый
тик драга). Плюс `Task.goal_id` (миграция `031_task_goal_link`) — прямая
копия уже принятого паттерна `Task.habit_id`: тот же опциональный
`goal_repository` в `TaskService`, тот же дропдаун в редакторе, тот же
бейдж-паттерн в строке задачи (🎯).

**Календарь.** `.cal-task.done` — было сознательное решение красить
серым (см. комментарий в коде прошлой сессии: "лишний цвет на
зачёркнутом только мешает"), владелец явно попросил зелёный —
переопределено на `.color-green`. Отдельно нашёлся реальный баг:
раскрытая панель дня по часам (`renderDayHours`) использовала только
`lastTasks` (активные задачи), никогда не подмешивала
`lastCompletedTasks` — тот же источник и тот же принцип "день по
`completed_at`", что уже использует сетка календаря
(`doneByDay` в `renderCalendar`), просто не был протянут в панель дня.

**Перегрузка интерфейса — три варианта, выбор, и сразу реализация на
одном домене.** Через скил `impeccable` подготовлены и представлены три
варианта архитектуры (меню-«⋮», свайп-жесты, разворачиваемая панель по
клику на строку) — владелец выбрал третий, плюс попросил добавить
свайпы как один из способов внутри него. Владелец сказал «готово
продолжай» сразу после выбора — решение не легло в бэклог, реализовано
в тот же день третьим коммитом, на задачах (первый из четырёх доменов).

`taskRow()`: было 5 кнопок (▶✎✓✕▾) всегда в строке — стало превью
(статус-точка+заголовок+бейджи+срок+✓+▾) и `.task-details` (▶✎✕ +
прежний `.task-panel` с подзадачами/комментариями без изменений внутри),
разворачивается кликом по строке (новый `data-action="toggle-row"` на
`.task-row-main`) ИЛИ по шеврону (старый `data-action="toggle-panel"`) —
оба пути сведены в один `toggleTaskRow()`, не задвоены. Плюс
`attachSwipeToComplete()` — свайп влево по строке на touch-устройствах
делает то же, что кнопка ✓ (hysteresis 8px не мешает вертикальному
скроллу, потолок -100px, порог -70px до срабатывания) — то, что владелец
попросил поверх варианта 3. Контакты/цели/долги нарочно не тронуты — см.
«Что сделать в начале новой сессии», п.1: сначала показать этот домен
живьём, потом либо править, либо тиражировать.

**Владелец проверил живьём, нашёл баг, подтвердил механику, разрешил
тиражировать — всё в одном заходе.** Скриншот: задача с двумя
незавершёнными подзадачами закрылась одним тапом по ✓, без единого
вопроса — "не хотел её закрывать". `completeTaskWithGuard()` — общая
точка для кнопки ✓ и свайпа: если есть незавершённые подзадачи, жёсткий
`alert`-блок (не предупреждение, которое можно проигнорировать), иначе
`confirm()` перед закрытием. Свайп заодно перестал играть slide-off
анимацию ДО реального успеха — раньше строка уезжала с экрана, даже
если гвард дальше блокировал закрытие. Following сразу: «а так всё норм,
делай для других» — контакты/цели/долги получили тот же приём
(`.task-row-main`/`.task-details`, свой `toggle*Row()` на домен, не
унифицирован в одну функцию — у каждого домена свой Set и свой
DOM-якорь панели). "Быстрое" действие снаружи — то, что было главным
для домена: 👋 у контактов, поле "Платёж"+✓ у долгов; у целей единого
одно-тапового действия и не было (нужен ползунок), там строка — чистое
превью без единой кнопки, всё за шевроном.

1303 теста (было 1293 на начало сессии, +10 — всё на goal_id; правки по
шкале настроения обновили существующие тесты, новых не добавили; вариант
3 — чистый `/ui` без бэкенда, тестов не прибавил), ruff/black чисты
после всех трёх коммитов.

**Три открытых вопроса под конец сессии — без кода, только разбор и
показ.** (1) «Голос не активируется» — оказалось про персонажей бота,
не баг; разобрано в п.2 выше. (2) `graffiti/logo.png` — владелец думал,
что уже сделано, проверка диска показала обратное — файл так и не
дошёл, см. п.3. (3) Фон скинов — три варианта представлены через
`impeccable` артефактом (реальные `decor-*`-ассеты groovy встроены как
data URI, не абстрактные мокапы), владелец выбрал вариант 2 к
реализации и вариант 3 — попробовать позже; ни один пока не в коде, см.
п.4.

---

(Ночь #6 25.08 — 8 волн правок по чек-листу владельца, фокус-сессии,
долги — сжато в архив, см. «Хронология фич» ниже.)

---

## Технические нюансы, добавившиеся за сессию

1. **Опрос БД вместо `job_queue.run_once` — общее правило, не
   разовое решение.** API и бот в этом проекте всегда будут разными
   процессами (docker-compose). Любая будущая фича с отложенным
   уведомлением/действием должна хранить МОМЕНТ времени в БД и
   опрашиваться повторяющейся джобой (как `list_due_unreminded`,
   `FocusSessionRepository.list_due_work_end`), а не полагаться на
   `context.job_queue.run_once()` — тот живёт только в памяти процесса
   бота и не переживает деплой.
2. **Кросс-доменная валидация через опциональный репозиторий в
   конструкторе сервиса** — паттерн `TaskService(repository,
   contact_repository=None)`: если валидатор передан (собран через
   `build_task_service` в `container.py`) — проверяет владение чужим
   id, если нет (прямая сборка `TaskService(TaskRepository(session))`
   в нескольких местах `jobs.py`/`handlers.py`/`callbacks.py`, AUDIT.md
   A-3) — молча пропускает. Тот же принцип опциональности, что уже был
   у `ai_client`/`contact_service` в `ConversationEngine`/`build_nudges`,
   применён теперь и к валидации, не только к включению фичи целиком.
3. **SQLite не хранит tzinfo** — при написании нового кода, сравнивающего
   datetime из БД с `datetime.now()`, если тесты идут на SQLite (а они
   всегда идут на SQLite, не на Postgres), нужно закладываться на то,
   что значение из БД может прийти naive, даже если писали aware. Общий
   приём — `datetime.now(value.tzinfo)`, не жёсткий `timezone.utc`
   (см. `to_local()` в `app/tasks/formatting.py`,
   `build_focus_message()` в `keyboards.py`).
4. **Новая иконка в SVG-наборе — можно ограничиться только базовым
   набором.** `glyphBody()` в `index.html` уже тихо откатывается на
   `ICON[name]`, если у активного скина (`pixel`/`japan`) своего
   варианта нет — не обязательно рисовать все три версии (`ICON`,
   `ICON_LINE`, `PIXEL_GRID`) для каждой новой иконки, прецедент уже
   был («people», теперь и «focus»).
5. Старые грабли (деплой только через `scripts/deploy.sh`,
   `pydantic-settings extra="forbid"`→`ignore`, `.claude/settings*.json`
   коммитит только владелец вручную, PATH для Node/Python не
   подхватывается в старой Bash-сессии, Windows+Bash — кириллица в
   `cd` иногда спонтанно фейлится) — без изменений.

---

## Состояние проекта

```bash
cd lifeos-agent
python -m pytest -q                                    # 1303 теста
python -m ruff check app tests migrations scripts
python -m black --check app tests migrations scripts
bash scripts/deploy.sh          # деплой (только так)
bash scripts/deploy.sh --check  # сверка прод/гит/локаль
```

Новое в структуре: `specs/022-tasks-v2.md`, `specs/026-focus-sessions.md`
(новые файлы), `specs/017-finance.md`/`specs/018-personal-crm.md`/
`specs/007-weekly-digest.md` дополнены разделами-довесками внутри тех
же файлов (не новые файлы — те фичи расширены, не переписаны).

Девятый и десятый домены: `app/focus/` (models/repository/service/
schemas — фокус-сессии), `Debt`/`DebtRepository`/`DebtService` внутри
`app/finance/` (не новый домен — довесок к существующему).
`app/api/charts.py` (новый роутер — PNG-график, без своего домена,
только эндпоинт поверх существующего `app/scheduler/charts.py`).
`app/api/focus.py`. Миграции `022`–`031` (tasks_v2 … task_goal_link).
`tests/focus/` (новый), `tests/crm/test_api.py`/`tests/finance/test_api.py`/
`tests/api/test_charts.py` (не было интеграционных REST-тестов для
этих доменов вообще, только сервис/репозиторий — закрыто попутно).

`app/web/static/index.html` — одиннадцатая вкладка/карточка «Фокус»,
панель подзадач/комментариев под строкой задачи, редактор задач
переиспользован для контактов, блоки «Долги»/«Аналитика» в карточке
«Финансы», график в «Итоги недели», индикатор «давно не заходил» на
вкладках, селектор шрифта в настройках. Один большой HTML-файл, как и
раньше, новых статических файлов нет (кроме уже существующих
`@font-face`, переиспользованных, не новых).

**Довески дня #7 (без нового домена):** `Task.goal_id` (миграция
`031_task_goal_link`, тот же паттерн, что `habit_id`/`contact_id`) —
дропдаун «Цель» в редакторе задачи, бейдж 🎯. `MIN_SCORE`/`MAX_SCORE` в
`app/mood/models.py` — единая точка правки диапазона настроения (сейчас
1-7), от неё зависят бот (`keyboards.py`, клавиатура строится циклом по
константам), matplotlib-график (`app/scheduler/charts.py`) и `/ui`
(константа `MOOD_MAX_SCORE` в JS — своя копия, как и раньше у эмодзи).

**День #8 — без нового домена и без миграций.** `specs/027-*`/
`specs/028-*` — новые файлы (черновики, не утверждены). `ROADMAP.md`
переписан целиком. `index.html`: фон groovy, graffiti-лого,
`toggleDomainRow()`. `app/core/container.py`/`jobs.py`/`handlers.py`/
`callbacks.py`: восемь мест переведены на `build_task_service()`.

---

## Что осталось

Смотри раздел «Что сделать в начале новой сессии» выше — по приоритету:
синхронизация (`git pull`), затем вёрстка Панели (вариант C выбран, не
реализован — это первое, что просил сделать владелец на новом
устройстве), затем два черновика спек ждут ответов владельца (персонажи
фаза 2, ферма/тамагочи), затем деплой трёх коммитов дня #8 на прод.

**Фаза 2 персонажей** — черновик спеки с открытыми вопросами написан
(`specs/027-butler-personas-phase2.md`), реализация ждёт ответов
владельца, не начата.

**Отложено осознанно:** перетаскивание плиток интерфейса, голосовой
ввод (до решения по монетизации и счётчика минут), аватар в шапке
(`designs/004-ui-layout.md` фиксирует отказ владельца от блока
профиля), десктоп-маскот тамагочи (отдельное приложение, после
бэкенда питомца).

**Из `AUDIT.md`:** P-3 (pgvector, после ~1000 записей памяти) — не
срочно. **A-3 для TaskService закрыт днём #8** — все восемь прямых
сборок `TaskService(TaskRepository(session))` в `jobs.py`/
`handlers.py`/`callbacks.py` переведены на `build_task_service()`.
Остальные однострочные сборки (`HabitService(HabitRepository(session))`
и т.п.) сознательно не тронуты — `container.py` документирует, что это
не то же самое дублирование, фабрика им не нужна.

---

# Архив прошлых сессий (сжато 25.08 — экономия токенов)

Полный построчный нарратив по каждой сессии до 23.08 удалён из этого
файла ради размера. Ниже — сухой остаток: решения и факты, которые
всё ещё могут понадобиться. Если нужны подробности конкретной правки —
смотреть `git log`, коммиты подписаны содержательно.

## Хронология фич (кратко, по сессиям)

**23.08, ночь #3.** Аудит кода (10 находок, коммит `83eaac2`) —
владелец задачи не проверялась в `mark_reminded`/напоминаниях, гонка в
`RewardsService.claim_today`, XSS в HTML-сообщениях без экранирования,
6 REST-роутеров переведены на фабрики `build_X_service`. Диагностика
удержания → Engagement Hooks (`specs/016`, коммит `f10b5cd4`): владелец
сказал прямым текстом, что реально пользуется только дайджестами
каналов — награда за любое «сделал»-действие по кнопке/в чате, не
только отдельный визит в `/ui`; утренняя рефлексия слита в
`send_morning_briefing_job`; кнопка «⭐ Сохранить» под дайджестом.
Финансы (`specs/017`, коммит `8b51cbf2` + `77978855`) — шестой домен,
свободные деньги = доход − обязательные, норма = процент от свободных,
разбор в чате без LLM, бот+REST API+`/ui` в один вечер (владелец не
нашёл кнопок без UI и попросил «полный арсенал»). Личный CRM + трекер
настроения (`specs/018`/`specs/019`, коммит `fec91fc`) — оба сразу с
REST API и `/ui`, прямой урок из истории с финансами. Картинки скинов
29/30 подключены (коммит `4b08c8a`) — `pngquant` не установлен на
машине, написан `scripts/process_skin_images.py` на Pillow.

**23.08, ночь #4.** Персонажи бота, фаза 1 (`specs/020-butler-personas.md`,
коммит `54dc15e6`) — восьмой домен `app/assistant/`, четыре
характер-листа, разговорный intent `CHAT` (сообщения с «?» или
приветствием больше не становятся задачей молча). Три мелких
улучшения без запроса конкретной фичи: «ещё N не поместилось» у всех
списков, бейджи закрытых тем подключены, LIMIT в БД для разговорного
контекста памяти и для `_recall` (тот же паттерн, что уже применялся к
чату).

**25.08, ночь #6.** Чек-лист из 8 находок (баги + недостающие функции) +
2 запроса на расширение, план через plan mode, 3 параллельных
Explore-агента на раскопки. 9 волн: CSS-баги (`.editor-row` без
`[hidden]`, `.task-actions` перенос кнопок, `CAL_FIRST_HOUR` 6→0),
кнопка ✓ у подзадачи в панели, таймер «в работе» (`in_progress_started_at`,
общий `registerTicker`-хелпер), CRM связанные задачи+комментарии
(`ContactComment` — копия `TaskComment`), `Task.habit_id` (копия
паттерна `contact_id`), живой таймер фокус-сессии (второй потребитель
`registerTicker`), «Итоги недели» на HTML/CSS вместо PNG, долги —
`DebtPayment` лог + план рассрочки (самая крупная волна), рекомендации
по бюджету (`build_budget_recommendations`, без AI-фразы). Попутный
баг: SQLite offset-naive datetime, тот же фикс, что уже был в
`nudges.py`. 1293 теста (было 1225).

**24.08, вечер/ночь #5.** Владелец скинул ~26 правок к `/ui` одним
потоком, попросил разобрать по важности и не останавливаться на
подтверждении каждого шага — десять коммитов подряд, каждый с тестами и
немедленным деплоем. Вёрстка (три подтверждённых геометрией бага:
смайлики настроения, окошко процента целей, кнопка дайджеста). Задачи
2.0 (`specs/022-tasks-v2.md`, коммит `d0dce29`) — подзадачи/эпики как
полноценные дочерние задачи, «в работе» отдельным полем `in_progress`
(не третий lifecycle-статус), `TaskComment`. Календарь — «✓ Выполнить»
из редактора, `GET /tasks/completed`. `Task.contact_id` (привязка к
CRM), CRM-довески (notes/tags/nudge_after_days + PATCH), Финансы: `Debt`
+ аналитика по месяцам, график «Итоги недели» на HTML/CSS вместо PNG,
индикатор «давно не заходил» (client-side), шрифты в настройках.
Фокус-сессии/Pomodoro (`specs/026-focus-sessions.md`, коммит `1d19336`)
— первая фича с опросом БД повторяющейся джобой вместо
`job_queue.run_once()` (API и бот разные процессы, см. «Технические
нюансы» выше), это стало общим архитектурным правилом на будущее. 1225
тестов (было 1067 на начало сессии).

**16-21.08.** Аудит безопасности (Postgres/API были доступны из
интернета, REST без аутентификации, токен бота утекал в логи, бот
отвечал кому угодно) — всё закрыто. Дайджест каналов без юзербота
(скрейпинг `t.me/s/`). Голосовой ввод выключен намеренно (единственная
статья расходов с заметной ценой, решение не меняется). `MULTIUSER.md`
написан — Google Drive не годится для мультиарендности, 152-ФЗ требует
серверов в РФ (сервер в Нидерландах — открытая архитектурная
проблема). TMDb/Google Books карточки, календарь, редизайн (даты
словами, мини-игра «Ежедневный визит»). REST API дайджестов, иконки
переработаны под 24px, скин `groovy` по дизайн-системе `designs/003`,
`japan` — старым способом (осознанная, не переделанная ошибка,
образцом для новых скинов брать `groovy`). Новая машина настроена по
`SETUP.md`, 5 design-скилов установлены. `graffiti` с нуля, `pixel`
слоты готовы (картинки — блокер, снят 23.08).

## Философия и архитектура (не менялись с 16.08)

**LifeOS Agent** — персональный AI Chief of Staff в Telegram,
single-user (не multi-tenant). Не чат-бот и не менеджер задач — цель
снять когнитивную нагрузку: бот сам держит контекст и проактивно
напоминает, а не только отвечает. Подробно — `PROJECT.md`.

ADR из `DECISIONS.md`: **ADR-001** модульный монолит, не микросервисы.
**ADR-002** LLM никогда не трогает БД напрямую, только через сервисный
слой. **ADR-003** spec-first — `specs/NNN` + `flows/NNN` до кода.
**ADR-004** простой код лучше LLM. **ADR-005** один сервис — одна
ответственность.

Слои: `API/Telegram → Service → Repository → DB`. `ConversationEngine`
Telegram-агностичен. Каждый домен — свои
`models.py`/`repository.py`/`service.py`/`schemas.py`; REST-роутеры
отдельно, в `app/api/`. **API и бот — разные процессы** (docker-compose
`lifeos-api`/`lifeos-bot`) — новое явное правило этой сессии, см.
«Технические нюансы» выше: ничто в памяти одного не видно другому,
отложенные действия — только через БД + опрос.

## Технический долг, который никуда не делся

Из `AUDIT.md`, архитектурный блок: **A-1** `telegram_user_id` не
проверяется как владелец сущности в сервисных методах, работающих по
`id` — частично закрыто новым паттерном (см. «Технические нюансы»,
п.2), но только там, где сервис собран через фабрику `build_X_service`,
не в прямых сборках. **A-3** фабрика сервисов вместо ручной сборки в
семи+ местах — не сокращается, новые фичи добавляют места, где выбор
уже стоял (фабрика vs прямая сборка), и решение не всегда в пользу
фабрики. **A-4** `EngineResult` вместо парсинга ответа регуляркой.
Дедупликация репозиториев (`BaseRepository[T]`), мёртвый код. **P-3**
pgvector — не срочно. **B-6/B-7/B-8** границы слов в парсере дат —
закрыты ещё 15.08.

Из старых идей, не начато: рецепты по фото продуктов (Media Inbox),
Weekly Review как отдельный диалог, проверить отмену пробного VPS
SmartApe (Москва, старый заброшенный).

---

## Доступы, пароли, ссылки

### Репозиторий
[gezhenko95-jpg/LifeOS-Agent](https://github.com/gezhenko95-jpg/LifeOS-Agent),
ветка `main`. Локально: путь меняется от машины к машине (см. `SETUP.md`), код в
`lifeos-agent/`.

### Продакшн-сервер (Hostkey, Нидерланды)
- IP: `148.135.208.126`, домен: `lifeos-agent.ru`
- SSH: `ssh lifeos-eu` (алиас в `~/.ssh/config`, ключ
  `~/.ssh/lifeos_main`). Вход только по ключу.
- Проект на сервере: `/opt/lifeos/`.
- Caddy — реверс-прокси + HTTPS, `/etc/caddy/Caddyfile`, проксирует
  `lifeos-agent.ru` → `localhost:8000`.

### Домен и DNS (SmartApe)
- Аккаунт: `gezhenko95@gmail.com`, панель `cp.smartape.ru`.
- Домен `lifeos-agent.ru`, до 2027-08-14.
- DNS-записи — отдельная панель: **https://dns-01.smartape.ru:1501**,
  логин `user1764200`, пароль — **в менеджере паролей** (запись
  «SmartApe DNS»). В этот файл секреты не вписывать: HANDOFF целиком
  вставляется в чаты, а значит уходит наружу вместе с паролями.

### Старый VPS (SmartApe, Москва) — заброшен
IP `188.127.224.144`, алиас `lifeos-vps` — не использовать, с
российских VPS заблокирован `api.telegram.org`.

### Google Drive (Media Inbox)
`credentials.json` + `token.json` в `/opt/lifeos/`, не в git. Проект
`LifeOS Agent` (`lifeos-agent-505510`), scope `drive.file`.
Переавторизация — `scripts/drive_auth.py` (локально, не в контейнере).

### Полезные ссылки
- OpenRouter: https://openrouter.ai
- Hostkey: https://hostkey.ru
- SmartApe: https://www.smartape.ru
- Google Cloud Console: https://console.cloud.google.com

## Технические грабли (актуальны)

1. Windows + Bash-инструмент: `cd` в кириллический путь иногда
   спонтанно фейлится — повторить без `cd`.
2. SSH-ключ в веб-форме хостера — вставлять вручную одной строкой.
3. Postgres в Docker: `POSTGRES_PASSWORD` из `docker-compose.yml`
   применяется только при первой инициализации пустого volume.
4. `echo "x" >> .env` без финального переноса строки — значение
   приклеится к предыдущей строке.
5. JSON-колонки в SQLAlchemy: `JSON(none_as_null=True)`, если значение
   может быть `None` и по нему потом фильтруют `IS NULL`.
6. uvicorn за реверс-прокси: всегда `--proxy-headers
   --forwarded-allow-ips=*`.
7. Алембик: id ревизии ≤32 символов.
8. SQLite (тесты) не хранит tzinfo — datetime из БД может прийти
   naive, даже если писали aware. `datetime.now(value.tzinfo)`, не
   жёсткий `timezone.utc`, при сравнении.
9. API и бот — разные процессы (docker-compose). Отложенные
   действия/уведомления — только опрос БД повторяющейся джобой, не
   `context.job_queue.run_once()`.
10. **CSS-специфичность скинов бьёт по новым компактным контролам —
    встречалось уже трижды (`.tab.active`, `.segmented-btn`,
    `.mood-picker button`).** `:root[data-skin="groovy"] button`/
    `graffiti` (0,2,1) специфичнее любого одноклассового селектора
    (0,1,0) и переопределяет его padding/min-height/border-radius, даже
    если у своего компонента специфичность 0,2,0 (`.tab.active`) — счёт
    идёт по каждому уровню тройки отдельно, класс+класс всё равно
    проигрывает класс+класс+элемент. Любая НОВАЯ маленькая кнопка
    (не `.secondary`/`.danger`, у которых оверрайд уже есть) на groovy/
    graffiti должна сразу получить свой `:root[data-skin="..."] .my-btn`
    оверрайд, а не полагаться на компактный CSS самого компонента.
11. **Многослойный `background-image` с несколькими `url()` через
    запятую в одном свойстве не парсился браузером (воспроизведено
    трижды 26.08: пропадал из `document.styleSheets` целиком, при этом
    `insertRule()` с тем же текстом отрабатывал без ошибок).** Причина не
    установлена (не порядок объявлений, не соседний код — изолированная
    минимальная проверка тоже не парсилась). Надёжный обход: раскладывать
    слои на отдельные селекторы (`::before`/`::after`/дочерние
    элементы) — один слой на одно правило, тот же приём, что уже
    используют `.card::before`/`::after` для углового декора карточек.
12. **Вкладка браузерного preview-инструмента может держать
    закэшированную версию страницы, даже когда файл на диске и HTTP-
    сервер уже отдают свежий контент.** При «правка не отражается в
    превью» — сначала `fetch(url, {cache:'no-store'})` и сравнить с
    ожидаемым текстом, только потом искать баг в CSS/JS. `navigate` с
    `force:true` не всегда сбрасывает кэш конкретной вкладки, если
    страница не запрашивалась заново (см. также грабли Windows+Bash
    выше — похожая ловушка «чиню не то»).

## Архитектура (не менялась)

Слои: `API/Telegram → Service → Repository → DB`. `ConversationEngine`
Telegram-агностичен. Каждый домен — свои
`models.py`/`repository.py`/`service.py`/`schemas.py`; REST-роутеры
отдельно, в `app/api/`.
