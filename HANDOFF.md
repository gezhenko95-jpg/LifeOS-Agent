# LifeOS Agent — Handoff (2026-08-25, ночь #6)

Вставьте этот файл целиком в начало нового чата с Claude, чтобы
продолжить работу без потери контекста. Ниже раздела «более ранние
записи» — история, она не пересматривалась.

Прод: коммит `0bc31eb`, 1293 теста, ruff и black чистые. Восемь волн
подряд за эту сессию — владелец продиктовал по пунктам живую проверку
прошлой сессии (баги вёрстки + недостающие функции), план был согласован
через plan mode заранее (см. `.claude/plans` в истории разговора), потом
каждая волна — свой коммит с тестами и немедленным деплоем
(`/health` проверен curl'ом после каждого из восьми).

> **То же предупреждение, что и в прошлой сессии, слово в слово:**
> задеплоено и покрыто тестами, но **живой проверки глазами/руками
> владельца по большинству новых кусков ещё не было** — часть пунктов
> ниже как раз и была ответом на предыдущий раунд живой проверки, но
> сами волны 2-8 этой ночи проверены только на уровне API/тестов, не в
> реальном Telegram/браузере.

---

## Что сделать в начале новой сессии

**0. Живая проверка — приоритет №1, список по волнам этой сессии:**

- **Волна 0 (вёрстка):** открыть `/ui` на панели уже сжатой ширины —
  редактор задачи (✎) не должен показывать «Теги»/«Нэдж» (поля
  контакта), кнопки задачи (▶✎✓✕▾) не должны наезжать друг на друга,
  календарь на 24-е число (или любой день с задачей без времени) должен
  показывать её в списке часов (в строке «00:00»).
- **Волна 1 (подзадачи):** в панели задачи (📎) у незавершённой подзадачи
  должна быть кнопка ✓ — жмём, подзадача зачёркивается.
- **Волна 2 («в работе»):** кнопка ▶ у задачи — должна появиться
  мигающая точка + таймер мм:сс рядом с названием, тикает раз в секунду.
- **Волна 3 (CRM):** у контакта в «Люди» — кнопка ▾ открывает панель со
  связанными задачами (кликабельны, открывают редактор) и комментариями
  (форма добавления снизу).
- **Волна 4 (задача↔привычка):** в редакторе задачи — новая строка
  «Привычка», выпадающий список из активных привычек; бейдж 🔁 в строке
  задачи, если привязана.
- **Волна 5 (фокус):** начать фокус-сессию (бот или `/ui`) — на `/ui`
  должен появиться живой таймер мм:сс и «тлеющая» шкала, меняющая цвет
  от акцентного к красному по мере убывания времени, мерцает в
  последние 20%. Сама механика уведомлений (сообщения в Telegram)
  подтверждена рабочей ещё ДО этой сессии (тестовая 1-минутная сессия
  владельца прошла оба перехода) — ЖИВЬЁМ смотреть нужно только новый
  фронтенд, не гнаться за самим ботом.
- **Волна 6 («Итоги недели»):** карточка на `/ui` теперь без картинки —
  HTML-бары, сверху `<select>` диапазона недель (4/8/12/26) и
  переключатель Задачи/Привычки/Настроение. Проверить оба фильтра и все
  три вида (привычки — мини-heatmap, настроение — столбики).
- **Волна 7 (долги):** в форме долга — два новых необязательных поля
  (платёж/мес., след. платёж). У каждого долга — кнопка ▾ открывает
  панель: график истории платежей + форма плана (сохранить/очистить).
- **Волна 8 (рекомендации):** блок «Рекомендации» в «Финансы» — если
  трат меньше 3 месяцев по категориям, покажет заглушку «мало истории»,
  это ожидаемо, не баг (нужно накопить историю трат, чтобы увидеть
  цифры).

**1. Персонажи бота — по-прежнему не проверены** (см. предыдущие
сессии, `specs/020-butler-personas.md`).

**2. `graffiti/logo.png` — владелец прислал сгенерированную картинку в
чат** (слово `LIFEOS` без лишнего текста, что и требовалось), но файл на
диск ещё не попал и альфа-канал не проверен — нужно получить сам файл,
проверить прозрачность (`getchannel("A").getextrema()`, не на глаз),
положить в `app/web/static/skins/graffiti/logo.png`, раскомментировать
`.brand::before` в `index.html` (комментарий на месте).

**3. Фон скинов — новая идея от владельца, не начато.** «Большая
объёмная картина» на задний план каждого скина — сам владелец сомневался,
что нужна именно иллюстрация. Перед кодом стоит открыть design-скилы
(`impeccable`/`paint`, подключены в `.claude/skills/`) и предложить
2-3 конкретных направления с превью в браузере — самый неопределённый
пункт из всех, специально оставлен напоследок.

**4. Старый бэклог (23-24.08, ~26 пунктов) — по-прежнему три
незакрытых:** вёрстка панели вкладок (нужно сначала посмотреть глазами,
что не так), ферма+тамагочи+магазин наград (самое крупное, отдельная
многочасовая задача, решения владельца уже зафиксированы в архиве ниже),
картинки (граффити-лого — см. п.2, доразрешение декора целей, недостающие
иконки финансов — ждут владельца).

---

## Что сделано в этой сессии (25.08, ночь #6)

Владелец продиктовал по пунктам чек-лист живой проверки из HANDOFF
прошлой сессии — 8 конкретных находок (баги + недостающие функции) плюс
два запроса на расширение (фокус-сессии, финансы/долги). План
согласован через plan mode перед стартом (раскопки — 3 параллельных
Explore-агента по задачам/календарю, CRM/связям, фокусу/графикам).

**Волна 0 — CSS-баги (index.html, без миграций).** Три находки с точной
причиной: `.editor-row` не имел пары `[hidden]{display:none}` — редактор
задачи реально показывал поля контакта, JS-логика видимости была верной,
дело было в CSS; 5 кнопок задачи обёрнуты в `.task-actions` с
`flex-basis:100%` (тот же приём, что уже был у `.task-panel`) — раньше
переносились по одной вперемешку с остальной строкой; `CAL_FIRST_HOUR`
6→0 — задача без явного времени (полночь) выпадала из списка дня.

**Волна 1 — завершение подзадачи из панели.** Зачёркивание уже
существовало (`.task-panel-done`), не хватало кнопки ✓ у незавершённой
подзадачи — добавлена, переиспользует тот же PATCH, что у обычной
задачи.

**Волна 2 — таймер «в работе».** `Task.in_progress_started_at`
(миграция 027) — мигающая точка + живой таймер мм:сс. Написан общий
`registerTicker`-хелпер (один `setInterval` на всю страницу вместо
одного на элемент) — переиспользован дальше в волне 5 для фокус-сессий.

**Волна 3 — CRM: связанные задачи + комментарии.**
`TaskRepository.list_by_contact` (обратная сторона `contact_id`),
`ContactComment` — прямая копия `TaskComment` (миграция 028). У
контакта — тот же toggle-panel паттерн, что у задачи.

**Волна 4 — задача↔привычка.** `Task.habit_id` (миграция 029) — прямая
копия уже принятого паттерна `contact_id` (тот же опциональный
`habit_repository` в `TaskService`, та же гейтинг-логика в редакторе).

**Волна 5 — живой таймер фокус-сессии.** Бэкенд не трогали — проверка
на проде (SSH, read-only) показала, что джоба и переходы работа→
перерыв→завершено уже работали правильно (тестовая 1-минутная сессия
владельца прошла оба перехода). Фронт — второй потребитель
`registerTicker`: живой отсчёт + «тлеющая» шкала (`color-mix` от
`--accent` к `--overdue`, мерцание в последние 20%).

**Волна 6 — «Итоги недели» HTML/CSS вместо PNG.** `GET
/charts/weekly/data` — JSON-версия уже существующего `gather_chart_data`
(теперь принимает `weeks=`), PNG-роут для дайджеста Telegram не тронут.
`/ui`: задачи переиспользуют `.fa-bar` язык финансовой аналитики
(третий раз тот же CSS, ни одной новой реализации графика), привычки —
мини-heatmap, настроение — столбики. Два фильтра: диапазон недель и тип
данных.

**Волна 7 — долги: лог платежей + план рассрочки (самая крупная).**
`DebtPayment` — append-only лог (миграция 030), пишется в
`record_payment` помимо уменьшения `remaining_amount`.
`Debt.monthly_payment`/`next_payment_due` — план, владелец двигает
вручную (авто-scheduler сознательно не строили). У каждого долга —
панель с графиком истории (те же `.fa-bar`, третье переиспользование) и
формой плана.

**Волна 8 — рекомендации по бюджету.**
`build_budget_recommendations(months=3)` — среднее по необязательной
категории за закрытые месяцы (текущий не в счёт), `suggested_cap =
avg_monthly`, без AI-фразы (решение владельца). Попутно найден и
починен реальный баг: SQLite отдавал offset-naive datetime из
`DateTime(timezone=True)`-колонки, сравнение с aware datetime падало —
тот же фикс, что уже был в `nudges.py`/`keyboards.py`.

Сквозная тема сессии: почти каждая новая связь/панель — не новый код с
нуля, а копия уже принятого паттерна (`contact_id`→`habit_id`,
`TaskComment`→`ContactComment`/`DebtPayment`, `.fa-bar` переиспользован
трижды, `.task-panel` toggle — четвёртый раз).

1293 теста (было 1225 в начале сессии, +68), ruff/black чисты после
каждой волны.

---

## Что сделано в прошлой сессии (24.08, вечер/ночь #5)

Контекст: владелец скинул скриншотами ~26 отдельных правок к `/ui` одним
потоком («пока всё») и попросил разобрать по важности и делать без
остановки на подтверждение каждого шага («как всё добавим — будем
делать тесты»). Ниже — в порядке, в котором фактически шли коммиты.
Все шаги — тесты + ruff/black + деплой с проверкой `/health` сразу
после, без пропусков.

### Вёрстка (коммит `0d0411e`)
Три подтверждённых измерением геометрии бага: смайлики настроения
вылезали на ~70px за карточку (flex без wrap), окошко процента у целей
обрезало число (56px, не хватало на groovy-паддинги), кнопка «Прислать
новое» у дайджестов могла сжиматься уже своего текста. Плюс список
каналов дайджеста сворачивается при >3.

### Задачи 2.0 (коммит `d0dce29`, `specs/022-tasks-v2.md`)
Три развилки, уточнённые заранее вопросами (не додуманы): подзадачи —
полноценные дочерние задачи со своим статусом (не чек-лист), «большая
задача» — это и есть задача с подзадачами (эпик = та же механика,
отдельного типа не заводили), «в работе» — отдельное поле
`in_progress`, НЕ третье значение lifecycle-статуса (иначе пришлось бы
переписывать все запросы `status=="active"`). `TaskComment` — лог из
нескольких записей. Список задач больше не показывает подзадачи
вперемешку (`top_level_only`), но текстовый поиск их по-прежнему
находит. Бот: `t|i`/`t|a`/`t|k`, бейджи 📎/💬 в тексте. Попутно найден и
починен реальный баг с реального использования: `send_midday_checkin_job`
выбрасывал текст со списком привычек, оставляя кнопки-цифры без
подписи, на что они отвечают.

### Календарь (коммит `b5483f3`)
«✓ Выполнить» в редакторе задачи (раньше открыть задачу из календаря
можно было только на правку). `GET /tasks/completed` — существующий
сервисный метод (Personal Insights) наконец получил роут. Выполненные
задачи зачёркнутым хвостом в клетке дня, по дню `completed_at`, не
`due_date`.

### Просроченные отдельной графой (коммит `7a82b27`)
Чистая `/ui`-правка — `dueStatus()` уже считал просрочку на клиенте,
только вёрстка: список разбит на «⚠️ Просрочено (N)» и «Остальные».

### Привязка задачи к контакту (коммит `1900b18`)
`Task.contact_id`, `ON DELETE SET NULL`. Кросс-доменная валидация —
`TaskService` получил опциональный `contact_repository` (паттерн
`ai_client` у `ConversationEngine`); прямые сборки `TaskService(...)` в
`jobs.py`/`handlers.py`/`callbacks.py` (AUDIT.md A-3, не
отрефакторено) её не получают — существующий, не новый trade-off.
Только `/ui`, в бота не выведено.

### CRM-довески (коммит `71f931b`, `specs/018`, продолжение)
`notes` существовал в модели с самого начала specs/018, но не было ни
`update_contact`, ни PATCH-роута, ни UI для правки — реальная дыра, не
пожелание. Плюс `tags` (свободная строка) и `nudge_after_days` (свой
порог «давно не писал» вместо глобальных 30). `/ui`: редактор задач
переиспользован и для контактов (`name`↔`title`, `notes`↔`description`
на уровне диалога).

### Финансы: долги + аналитика (коммит `78bbff2`, `specs/017`, продолжение)
`Debt` — отдельная сущность от `Transaction` (долг живёт месяцами и
имеет остаток), в расчёт свободных денег НЕ входит — осознанно, чтобы
не переусложнять уже принятую владельцем формулу. `monthly_breakdown` —
доход/траты по месяцам, месяцы без транзакций тоже в списке нулями
(график не должен «прыгать»). `/ui`: блок «Долги» (прогресс-бар,
частичная оплата) и «Аналитика» (две полоски на месяц, тот же
визуальный язык, что `.progress-bar` остальной страницы). В бота не
выведено.

### График в «Итоги недели» (коммит `b3a213a`, `specs/007`, довесок)
Новый роут `/charts/weekly`, БЕЗ дублирования отрисовки — тот же
`gather_chart_data`/`render_chart`, что уже строит PNG для дайджеста в
Telegram. Личные данные — эндпоинт под общим токеном, но `<img src>` не
умеет слать заголовки: `/ui` тянет картинку через `fetch()`+blob URL
(`loadWeeklyChart`), не голым `<img src="/charts/weekly">`.

### «Давно не заходил» (коммит `d2a451a`)
Чисто client-side (localStorage) — не заводили бэкенд-таблицу
last_visit ради личной привычки одного пользователя. Точка на вкладке,
порог 7 дней, «Панель» (мозаика всех карточек) в подсчёт не входит.

### Шрифты в настройках (коммит `775a1f1`)
4 пресета (системный/круглый/классический/пиксельный), действует
только у «Обычного» оформления — у скинов типографика часть
задуманного образа, отдельный переключатель её не трогает.
Круглый/пиксельный переиспользуют уже загруженные `@font-face`
(Comfortaa/Pixelify Sans скинов groovy/pixel), новых файлов нет.

### Фокус-сессии/Pomodoro (коммит `1d19336`, `specs/026-focus-sessions.md`)
Последний из трёх пунктов, что владелец просил ещё в бэклоге предыдущей
сессии (после personal CRM и mood tracker). Решения были уточнены
заранее вопросами: старт — кнопка 25 мин по умолчанию + своя
длительность; полный цикл — бот считает и работу, и перерыв, уведомляет
по концу обоих; привязка к задаче опциональна; статистика — карточка
`/ui` + строка в воскресном дайджесте.

**Архитектурное решение, которого не было ни у одной прошлой фичи:**
опрос БД повторяющейся джобой (`send_focus_notifications_job`,
интервал 20с), а НЕ `context.job_queue.run_once()`. Причина —
API и бот в этом проекте разные процессы (отдельные контейнеры
docker-compose): in-memory таймер бота не виден из API (нельзя было бы
отменить/увидеть сессию, заведённую через `/ui`) и не переживает
рестарт бота, а деплой перезапускает контейнер бота на каждую правку —
за одну эту сессию бот передеплоивался десять раз. Тот же приём и
довод, что уже был у `list_due_unreminded` для задач/привычек, просто
впервые письменно проговорено явно как общее архитектурное правило —
стоит иметь в виду для любых будущих фич с таймерами (например, для
фермы/тамагочи, если там появится что-то по времени).

Один активный сеанс на пользователя. Домен-буква бота — `z`. Пункт
меню «⏱ Фокус» ведёт не в статичный экран «список+добавить» (как у
остальных доменов), а сразу в живой статус — потому что «список»
нечего открывать, сессия максимум одна. Новая иконка (циферблат) в
общий SVG-набор — только базовый вариант, pixel/japan скины тихо
откатываются на него же (тот же приём, что уже был у иконки «people»).

Попутный баг, не из этой фичи конкретно, но найден при её тестах:
SQLite не хранит tzinfo, обратно приходит naive datetime — то же самое,
что `to_local()` в `app/tasks/formatting.py` уже когда-то чинил для
дат задач. Починено тем же приёмом (now() того же tzinfo, что и
сравниваемое значение).

**Побочный вопрос от владельца, отвеченный, но не реализованный:**
десктоп-маскот для тамагочи (жанр Shimeji) — реализуемо, но это
отдельное Windows-приложение вне текущего веб-стека, делать после
бэкенда питомца/фермы, см. раздел «Что осталось» выше.

1225 тестов (было 1067 на начало сессии), ruff/black чисты весь путь.

---

## Технические нюансы, добавившиеся за сессию

1. **Опрос БД вместо `job_queue.run_once` — общее правило, не
   разовое решение.** API и бот в этом проекте всегда будут разными
   процессами (docker-compose). Любая будущая фича с отложенным
   уведомлением/действием должна хранить МОМЕНТ времени в БД и
   опрашиваться повторяющейся джобой (как `list_due_unreminded`,
   `FocusSessionRepository.list_due_work_end`), а не полагаться на
   `context.job_queue.run_once()` — тот живёт только в памяти процесса
   бота и не переживает деплой.
2. **Кросс-доменная валидация через опциональный репозиторий в
   конструкторе сервиса** — паттерн `TaskService(repository,
   contact_repository=None)`: если валидатор передан (собран через
   `build_task_service` в `container.py`) — проверяет владение чужим
   id, если нет (прямая сборка `TaskService(TaskRepository(session))`
   в нескольких местах `jobs.py`/`handlers.py`/`callbacks.py`, AUDIT.md
   A-3) — молча пропускает. Тот же принцип опциональности, что уже был
   у `ai_client`/`contact_service` в `ConversationEngine`/`build_nudges`,
   применён теперь и к валидации, не только к включению фичи целиком.
3. **SQLite не хранит tzinfo** — при написании нового кода, сравнивающего
   datetime из БД с `datetime.now()`, если тесты идут на SQLite (а они
   всегда идут на SQLite, не на Postgres), нужно закладываться на то,
   что значение из БД может прийти naive, даже если писали aware. Общий
   приём — `datetime.now(value.tzinfo)`, не жёсткий `timezone.utc`
   (см. `to_local()` в `app/tasks/formatting.py`,
   `build_focus_message()` в `keyboards.py`).
4. **Новая иконка в SVG-наборе — можно ограничиться только базовым
   набором.** `glyphBody()` в `index.html` уже тихо откатывается на
   `ICON[name]`, если у активного скина (`pixel`/`japan`) своего
   варианта нет — не обязательно рисовать все три версии (`ICON`,
   `ICON_LINE`, `PIXEL_GRID`) для каждой новой иконки, прецедент уже
   был («people», теперь и «focus»).
5. Старые грабли (деплой только через `scripts/deploy.sh`,
   `pydantic-settings extra="forbid"`→`ignore`, `.claude/settings*.json`
   коммитит только владелец вручную, PATH для Node/Python не
   подхватывается в старой Bash-сессии, Windows+Bash — кириллица в
   `cd` иногда спонтанно фейлится) — без изменений.

---

## Состояние проекта

```bash
cd lifeos-agent
python -m pytest -q                                    # 1225 тестов
python -m ruff check app tests migrations scripts
python -m black --check app tests migrations scripts
bash scripts/deploy.sh          # деплой (только так)
bash scripts/deploy.sh --check  # сверка прод/гит/локаль
```

Новое в структуре: `specs/022-tasks-v2.md`, `specs/026-focus-sessions.md`
(новые файлы), `specs/017-finance.md`/`specs/018-personal-crm.md`/
`specs/007-weekly-digest.md` дополнены разделами-довесками внутри тех
же файлов (не новые файлы — те фичи расширены, не переписаны).

Девятый и десятый домены: `app/focus/` (models/repository/service/
schemas — фокус-сессии), `Debt`/`DebtRepository`/`DebtService` внутри
`app/finance/` (не новый домен — довесок к существующему).
`app/api/charts.py` (новый роутер — PNG-график, без своего домена,
только эндпоинт поверх существующего `app/scheduler/charts.py`).
`app/api/focus.py`. Миграции `022`–`026` (tasks_v2, task_contact_link,
crm_tags_nudge, finance_debts, focus_sessions). `tests/focus/` (новый),
`tests/crm/test_api.py`/`tests/finance/test_api.py`/
`tests/api/test_charts.py` (не было интеграционных REST-тестов для
этих доменов вообще, только сервис/репозиторий — закрыто попутно).

`app/web/static/index.html` — одиннадцатая вкладка/карточка «Фокус»,
панель подзадач/комментариев под строкой задачи, редактор задач
переиспользован для контактов, блоки «Долги»/«Аналитика» в карточке
«Финансы», график в «Итоги недели», индикатор «давно не заходил» на
вкладках, селектор шрифта в настройках. Один большой HTML-файл, как и
раньше, новых статических файлов нет (кроме уже существующих
`@font-face`, переиспользованных, не новых).

---

## Что осталось

Смотри раздел «Что сделать в начале новой сессии» выше — три пункта:
живая проверка ВСЕГО, что задеплоено за эту сессию (приоритет №1),
вёрстка панели вкладок (нужно сначала увидеть баг глазами), и
ферма+тамагочи+магазин (самое крупное, отдельная многочасовая задача,
решения владельца уже собраны).

**Не подтверждено живым использованием:** буквально всё, что
перечислено в «Что сделано в этой сессии» выше — десять коммитов, ноль
живых проверок. Это самая большая накопленная «яма недоверенного» за
всю историю проекта — стоит закрыть её раньше, чем копить ещё.

**Фаза 2 персонажей** (не начата, не менялась эту сессию) — более
тесная память диалога, редкие незапланированные сообщения, собственные
имена, см. `specs/020-butler-personas.md`.

**Отложено осознанно:** перетаскивание плиток интерфейса, голосовой
ввод (до решения по монетизации и счётчика минут), аватар в шапке
(`designs/004-ui-layout.md` фиксирует отказ владельца от блока
профиля), десктоп-маскот тамагочи (отдельное приложение, после
бэкенда питомца).

**Из `AUDIT.md`:** P-3 (pgvector, после ~1000 записей памяти) — не
срочно. A-3 (фабрика сервисов вместо ручной сборки в семи местах) —
за эту сессию картина не изменилась: новые фичи, где нужна была
кросс-доменная проверка (задача↔контакт), используют фабрику
`build_task_service`, но старые прямые сборки `TaskService(...)` в
`jobs.py`/`handlers.py`/`callbacks.py` по-прежнему не отрефакторены —
живой, не новый долг.

---

# Архив прошлых сессий (сжато 24.08 — экономия токенов)

Полный построчный нарратив по каждой сессии до 23.08 удалён из этого
файла ради размера. Ниже — сухой остаток: решения и факты, которые
всё ещё могут понадобиться. Если нужны подробности конкретной правки —
смотреть `git log`, коммиты подписаны содержательно.

## Хронология фич (кратко, по сессиям)

**23.08, ночь #3.** Аудит кода (10 находок, коммит `83eaac2`) —
владелец задачи не проверялась в `mark_reminded`/напоминаниях, гонка в
`RewardsService.claim_today`, XSS в HTML-сообщениях без экранирования,
6 REST-роутеров переведены на фабрики `build_X_service`. Диагностика
удержания → Engagement Hooks (`specs/016`, коммит `f10b5cd4`): владелец
сказал прямым текстом, что реально пользуется только дайджестами
каналов — награда за любое «сделал»-действие по кнопке/в чате, не
только отдельный визит в `/ui`; утренняя рефлексия слита в
`send_morning_briefing_job`; кнопка «⭐ Сохранить» под дайджестом.
Финансы (`specs/017`, коммит `8b51cbf2` + `77978855`) — шестой домен,
свободные деньги = доход − обязательные, норма = процент от свободных,
разбор в чате без LLM, бот+REST API+`/ui` в один вечер (владелец не
нашёл кнопок без UI и попросил «полный арсенал»). Личный CRM + трекер
настроения (`specs/018`/`specs/019`, коммит `fec91fc`) — оба сразу с
REST API и `/ui`, прямой урок из истории с финансами. Картинки скинов
29/30 подключены (коммит `4b08c8a`) — `pngquant` не установлен на
машине, написан `scripts/process_skin_images.py` на Pillow.

**23.08, ночь #4.** Персонажи бота, фаза 1 (`specs/020-butler-personas.md`,
коммит `54dc15e6`) — восьмой домен `app/assistant/`, четыре
характер-листа, разговорный intent `CHAT` (сообщения с «?» или
приветствием больше не становятся задачей молча). Три мелких
улучшения без запроса конкретной фичи: «ещё N не поместилось» у всех
списков, бейджи закрытых тем подключены, LIMIT в БД для разговорного
контекста памяти и для `_recall` (тот же паттерн, что уже применялся к
чату).

**16-21.08.** Аудит безопасности (Postgres/API были доступны из
интернета, REST без аутентификации, токен бота утекал в логи, бот
отвечал кому угодно) — всё закрыто. Дайджест каналов без юзербота
(скрейпинг `t.me/s/`). Голосовой ввод выключен намеренно (единственная
статья расходов с заметной ценой, решение не меняется). `MULTIUSER.md`
написан — Google Drive не годится для мультиарендности, 152-ФЗ требует
серверов в РФ (сервер в Нидерландах — открытая архитектурная
проблема). TMDb/Google Books карточки, календарь, редизайн (даты
словами, мини-игра «Ежедневный визит»). REST API дайджестов, иконки
переработаны под 24px, скин `groovy` по дизайн-системе `designs/003`,
`japan` — старым способом (осознанная, не переделанная ошибка,
образцом для новых скинов брать `groovy`). Новая машина настроена по
`SETUP.md`, 5 design-скилов установлены. `graffiti` с нуля, `pixel`
слоты готовы (картинки — блокер, снят 23.08).

## Философия и архитектура (не менялись с 16.08)

**LifeOS Agent** — персональный AI Chief of Staff в Telegram,
single-user (не multi-tenant). Не чат-бот и не менеджер задач — цель
снять когнитивную нагрузку: бот сам держит контекст и проактивно
напоминает, а не только отвечает. Подробно — `PROJECT.md`.

ADR из `DECISIONS.md`: **ADR-001** модульный монолит, не микросервисы.
**ADR-002** LLM никогда не трогает БД напрямую, только через сервисный
слой. **ADR-003** spec-first — `specs/NNN` + `flows/NNN` до кода.
**ADR-004** простой код лучше LLM. **ADR-005** один сервис — одна
ответственность.

Слои: `API/Telegram → Service → Repository → DB`. `ConversationEngine`
Telegram-агностичен. Каждый домен — свои
`models.py`/`repository.py`/`service.py`/`schemas.py`; REST-роутеры
отдельно, в `app/api/`. **API и бот — разные процессы** (docker-compose
`lifeos-api`/`lifeos-bot`) — новое явное правило этой сессии, см.
«Технические нюансы» выше: ничто в памяти одного не видно другому,
отложенные действия — только через БД + опрос.

## Технический долг, который никуда не делся

Из `AUDIT.md`, архитектурный блок: **A-1** `telegram_user_id` не
проверяется как владелец сущности в сервисных методах, работающих по
`id` — частично закрыто новым паттерном (см. «Технические нюансы»,
п.2), но только там, где сервис собран через фабрику `build_X_service`,
не в прямых сборках. **A-3** фабрика сервисов вместо ручной сборки в
семи+ местах — не сокращается, новые фичи добавляют места, где выбор
уже стоял (фабрика vs прямая сборка), и решение не всегда в пользу
фабрики. **A-4** `EngineResult` вместо парсинга ответа регуляркой.
Дедупликация репозиториев (`BaseRepository[T]`), мёртвый код. **P-3**
pgvector — не срочно. **B-6/B-7/B-8** границы слов в парсере дат —
закрыты ещё 15.08.

Из старых идей, не начато: рецепты по фото продуктов (Media Inbox),
Weekly Review как отдельный диалог, проверить отмену пробного VPS
SmartApe (Москва, старый заброшенный).

---

## Доступы, пароли, ссылки

### Репозиторий
[gezhenko95-jpg/LifeOS-Agent](https://github.com/gezhenko95-jpg/LifeOS-Agent),
ветка `main`. Локально: путь меняется от машины к машине (см. `SETUP.md`), код в
`lifeos-agent/`.

### Продакшн-сервер (Hostkey, Нидерланды)
- IP: `148.135.208.126`, домен: `lifeos-agent.ru`
- SSH: `ssh lifeos-eu` (алиас в `~/.ssh/config`, ключ
  `~/.ssh/lifeos_main`). Вход только по ключу.
- Проект на сервере: `/opt/lifeos/`.
- Caddy — реверс-прокси + HTTPS, `/etc/caddy/Caddyfile`, проксирует
  `lifeos-agent.ru` → `localhost:8000`.

### Домен и DNS (SmartApe)
- Аккаунт: `gezhenko95@gmail.com`, панель `cp.smartape.ru`.
- Домен `lifeos-agent.ru`, до 2027-08-14.
- DNS-записи — отдельная панель: **https://dns-01.smartape.ru:1501**,
  логин `user1764200`, пароль — **в менеджере паролей** (запись
  «SmartApe DNS»). В этот файл секреты не вписывать: HANDOFF целиком
  вставляется в чаты, а значит уходит наружу вместе с паролями.

### Старый VPS (SmartApe, Москва) — заброшен
IP `188.127.224.144`, алиас `lifeos-vps` — не использовать, с
российских VPS заблокирован `api.telegram.org`.

### Google Drive (Media Inbox)
`credentials.json` + `token.json` в `/opt/lifeos/`, не в git. Проект
`LifeOS Agent` (`lifeos-agent-505510`), scope `drive.file`.
Переавторизация — `scripts/drive_auth.py` (локально, не в контейнере).

### Полезные ссылки
- OpenRouter: https://openrouter.ai
- Hostkey: https://hostkey.ru
- SmartApe: https://www.smartape.ru
- Google Cloud Console: https://console.cloud.google.com

## Технические грабли (актуальны)

1. Windows + Bash-инструмент: `cd` в кириллический путь иногда
   спонтанно фейлится — повторить без `cd`.
2. SSH-ключ в веб-форме хостера — вставлять вручную одной строкой.
3. Postgres в Docker: `POSTGRES_PASSWORD` из `docker-compose.yml`
   применяется только при первой инициализации пустого volume.
4. `echo "x" >> .env` без финального переноса строки — значение
   приклеится к предыдущей строке.
5. JSON-колонки в SQLAlchemy: `JSON(none_as_null=True)`, если значение
   может быть `None` и по нему потом фильтруют `IS NULL`.
6. uvicorn за реверс-прокси: всегда `--proxy-headers
   --forwarded-allow-ips=*`.
7. Алембик: id ревизии ≤32 символов.
8. SQLite (тесты) не хранит tzinfo — datetime из БД может прийти
   naive, даже если писали aware. `datetime.now(value.tzinfo)`, не
   жёсткий `timezone.utc`, при сравнении.
9. API и бот — разные процессы (docker-compose). Отложенные
   действия/уведомления — только опрос БД повторяющейся джобой, не
   `context.job_queue.run_once()`.

## Архитектура (не менялась)

Слои: `API/Telegram → Service → Repository → DB`. `ConversationEngine`
Telegram-агностичен. Каждый домен — свои
`models.py`/`repository.py`/`service.py`/`schemas.py`; REST-роутеры
отдельно, в `app/api/`.
