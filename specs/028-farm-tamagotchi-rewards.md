# Ферма + тамагочи + магазин наград (черновик, не утверждено)

Самый крупный пункт бэклога, копившийся с 23.08 без спеки — только
устные решения владельца, записанные в архиве `HANDOFF.md`. Это первая
попытка свести их в спецификацию по формату `specs/README.md`.
Реализация не начинается, пока владелец не закроет открытые вопросы
ниже (раздел «Открытые вопросы») — они реально меняют объём работы, не
косметика.

---

# Цель

Существующая механика наград (`app/rewards/` — ежедневный чек-ин,
монеты, стрик) — это касса, но тратить монеты пока некуда: они просто
накапливаются в счётчике. Решения владельца (см. архив, коммит истории
HANDOFF 23-24.08): монеты должны на что-то расходоваться, а не быть
самоцелью. Три связанных механики:

1. **Питомец (тамагочи)** — полноценный, не декоративный: у него есть
   состояние (голод/настроение), за ним нужно ухаживать.
2. **Ферма** — мини-игра добычи корма: семена/удобрения → сено → корм
   питомцу.
3. **Магазин наград** — тратить монеты на что-то (украшения питомца?
   ускорения фермы? прочее — см. открытые вопросы).

Отдельно — идея десктоп-маскота (Windows, жанр Shimeji/Desktop Goose):
подтверждена как реализуемая, но **явно вне рамок этой спеки** — это
отдельное Windows-приложение вне веб-стека `/ui`, делается после того,
как бэкенд питомца и фермы уже существует и стабилен.

---

# Важный архитектурный факт, меняющий объём

`RewardsService.total_coins` (см. `app/rewards/coins.py`) **не хранимый
баланс, а производное число** — пересчитывается с нуля из полной
истории чек-инов при каждом запросе (см. докстринг `total_coins`:
«каждый день учитывается с его собственным стриком... монеты — это
история, а не индикатор текущей формы»). Сейчас монеты нельзя потратить
вообще — нет места, где баланс мог бы уменьшиться.

Магазин требует **баланс, который реально расходуется**. Два варианта:

- **(A) Ledger поверх существующего.** Новая таблица
  `CoinTransaction(telegram_user_id, amount, reason, created_at)` —
  чек-ин пишет `+N` (или остаётся производным и просто читается как
  «доступно для начисления», см. ниже), покупка пишет `-N`. Баланс =
  `total_coins(checkin_days) − sum(spent)`. Не трогает существующий
  `total_coins`/`coins.py` вообще, только добавляет вычитание сверху.
- **(B) Полный переход на хранимый баланс.** `total_coins` перестаёт
  быть производным, чек-ин инкрементит поле `Wallet.balance` напрямую.
  Чище на будущее, но переписывает уже работающий и протестированный
  код без прямой необходимости.

**Рекомендация: (A)** — минимальное изменение, существующая механика
чек-ина не трогается вообще, только добавляется независимая таблица
трат и вычитание при чтении статуса.

---

# Открытые вопросы владельцу (блокируют реализацию)

1. **Магазин: что именно продаётся?** Варианты (не взаимоисключающие):
   украшения/скины для питомца, ускорители фермы (быстрее растёт
   урожай), косметика `/ui` (доп. скины/темы), что-то ещё? Без списка
   товаров магазин не спроектировать (модель `ShopItem` зависит от
   типов эффектов).
2. **Питомец: что значит «полноценная механика, не декоративная»
   конкретно?** Голод (снижается со временем, кормишь → растёт)?
   Настроение? Возможность «заболеть»/«погибнуть» при долгом
   пренебрежении, или мягкая механика без потерь? Один питомец на
   аккаунт или можно завести нескольких?
3. **Ферма: цикл семена→удобрения→сено→корм — сколько стадий и с какой
   скоростью?** Реальное время (грядка растёт N часов, как в
   классических фермах) или мгновенно по клику? Нужен хотя бы один
   реальный числовой пример от владельца, чтобы откалибровать баланс
   экономики (сколько сена даёт одна грядка, сколько корма ест питомец
   в день, сколько монет стоит семя).
4. **Домен-буква бота** (см. паттерн в `jobs.py`/`callbacks.py` — у
   каждого домена своя буква в `callback_data`) и пункт меню — нужен
   свободный слот.

Без ответов 1-3 нельзя спроектировать модели данных и API ниже —
раздел «Модель данных» помечен как черновой набросок именно поэтому.

---

# Архитектура (набросок, требует уточнения по вопросам выше)

Новый одиннадцатый домен `app/farm/` (по образцу `app/focus/`):
`models.py`/`repository.py`/`service.py`/`schemas.py`, REST-роутер
`app/api/farm.py`, фабрика `build_farm_service` в `container.py`.

Если ферма растёт в реальном времени — тот же паттерн, что уже стал
общим архитектурным правилом после фокус-сессий (см. `HANDOFF.md`,
«Технические нюансы», п.1): момент времени в БД (когда грядка была
посажена), опрос повторяющейся джобой для случаев, требующих
уведомления («сено готово») — НЕ `job_queue.run_once`, т.к. бот и API
разные процессы.

Магазин — вариант (A) выше: `CoinTransaction` (новая таблица),
`ShopItem` (каталог, вероятно захардкоженный список констант на первую
итерацию — по аналогии с тем, как персонажи в `personas.py` не таблица,
а константы, пока не понадобилась гибкость), `PetInventory` (что куплено).

Питомец — `Pet` (одна строка на пользователя или несколько, см. вопрос
2): `hunger`, `mood`, `last_fed_at`. Кормление — расходует «сено» (из
`FarmInventory` или аналог).

---

# Что НЕ входит (в любом случае)

- Десктоп-маскот (Shimeji/Desktop Goose) — отдельное приложение,
  отдельная спека, после того как бэкенд питомца/фермы стабилен.
- Голосовой ввод, мультиарендность — не связаны с этой фичей.

---

# План реализации (после ответов на открытые вопросы)

1. Владелец отвечает на вопросы 1-4.
2. Спека дополняется точной моделью данных и API (сейчас — набросок).
3. `app/farm/` — модели, миграция, сервис, репозиторий, тесты.
4. Магазин — `CoinTransaction`, `ShopItem`-каталог, эндпоинт покупки.
5. Питомец — `Pet`, кормление, голод/настроение.
6. `/ui` — новая вкладка/карточка (по аналогии с «Фокус» — одиннадцатая
   добавилась без проблем, двенадцатая пойдёт тем же путём).
7. Бот — команды/кнопки по необходимости (решить при уточнении вопроса
   3 — насколько ферма нуждается в проактивных уведомлениях от бота).

---

# Definition of Done

- Открытые вопросы 1-4 закрыты явным ответом владельца, зафиксированы в
  обновлённой версии этой спеки.
- Модели данных и API уточнены (не набросок).
- Реализовано, тесты зелёные, ruff/black чисты.
- Проверено вручную владельцем на `/ui`.
