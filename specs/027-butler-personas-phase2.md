# Butler Personas — Фаза 2 (черновик, не утверждено)

Продолжение [specs/020-butler-personas.md](020-butler-personas.md), раздел
«План реализации → Фаза 2»: три пункта, отложенные при первой итерации.
Этот файл — черновик для утверждения владельцем, реализация не
начинается, пока не согласован объём (см. `specs/README.md`).

---

# Цель

Фаза 1 дала персонажу голос в пяти существующих местах, но каждое
взаимодействие всё ещё «в вакууме»: разговорный фолбэк (`chat_reply.py`)
не помнит, что говорилось пять минут назад в этом же диалоге, персонаж
не пишет сам без повода, и обращается к владельцу без имени. Фаза 2 —
три независимых улучшения, каждое можно принять/отклонить по
отдельности.

---

# Бизнес-требования

## 1. Память диалога (недавние реплики, не только Memory)

- `chat_reply.py` сейчас берёт контекст из последних `MemoryEntry`
  (journal/preference/project) — это долгосрочная память, не история
  ЭТОГО разговора. Реплика «а второй вариант?» без сохранённого текста
  предыдущего ответа бота не разрешима.
- Хранить последние N реплик диалога (владелец + бот) в новой таблице
  или in-memory с TTL — **решение владельца нужно здесь**: постоянное
  хранение (переживает рестарт бота) vs оперативное (проще, дешевле,
  теряется при деплое). Рекомендация: постоянное — деплои случаются
  несколько раз в сессию, оперативное отваливалось бы посреди разговора.
- Окно ограничено (например, последние 6-10 реплик или последние N
  минут) — то же ограничение, что и у Memory-контекста в фазе 1: не
  «помни весь диалог», иначе стоимость промпта растёт без границ.

## 2. Редкие незапланированные сообщения персонажа

- Явно ОТКЛОНЕНО в фазе 1 («частоту push-сообщений не увеличиваем»,
  «случайные незапланированные сообщения вне расписания... сознательно
  отложено»). Фаза 2 поднимает вопрос заново — **нужно явное решение
  владельца**: действительно ли увеличивать число проактивных сообщений
  в день, и если да — на основе чего триггерится сообщение (замеченный
  паттерн? случайный слот раз в несколько дней? привязка к событию —
  например, стрик привычки оборвался)?
- Без ответа на этот вопрос пункт не начинается — иначе повторит уже
  однажды отклонённое решение.

## 3. Собственные имена персонажей

- Фаза 1: «ярлык роли, не имя» — осознанное решение против копирайта и
  китча. Фаза 2 могла бы дать персонажам имена (не привязанные к
  существующим медиа) — минимальное изменение: константа `name` в
  `personas.py` рядом с характер-листом, подставляется в промпт и,
  возможно, в переключатель на `/ui` (вместо/вместе с иконкой).
- Самый маленький и наименее спорный из трёх пунктов — можно делать
  независимо от 1 и 2.

---

# Что НЕ входит

- Голосовой ввод — по-прежнему не трогаем (specs/012).
- Переключатель персонажа в боте — по-прежнему только `/ui` (фаза 1,
  явное требование владельца).
- Векторный поиск/pgvector для памяти диалога — окно ограничено числом
  последних реплик, не семантическим поиском по всей истории.

---

# Открытые вопросы владельцу (до начала реализации)

1. Память диалога: постоянная таблица или оперативная (переживает
   рестарт бота или нет)?
2. Незапланированные сообщения: делаем вообще, или это снова
   отклоняется как в фазе 1? Если делаем — какой триггер и с какой
   частотой?
3. Имена персонажей: делаем — тогда нужны сами имена (4 штуки) от
   владельца, стиль как у характер-листов (не буквальные персонажи из
   медиа).

---

# Архитектура (набросок, зависит от ответов выше)

Если память диалога — постоянная: новая таблица `ConversationTurn`
(`telegram_user_id`, `role` bot|user, `content`, `created_at`), новый
метод в `app/assistant/repository.py` или отдельный модуль
`app/conversation/history.py` — `list_recent_turns(telegram_user_id,
limit)`. `chat_reply.py` подмешивает эти реплики к существующему
контексту (Memory + сводка дня) перед вызовом AI.

Незапланированные сообщения — тот же паттерн, что уже действует в
проекте (см. `HANDOFF.md`, «Технические нюансы»): опрос БД
повторяющейся джобой, не `job_queue.run_once`. Конкретный триггер
зависит от ответа на вопрос №2.

Имена — константа в `app/assistant/personas.py`, без миграции.

---

# Definition of Done

- Каждый из трёх пунктов реализован независимо, только после ответа
  владельца на соответствующий открытый вопрос.
- Тесты зелёные, ruff/black чисты.
- Проверено вручную владельцем на `/ui` и в боте.
